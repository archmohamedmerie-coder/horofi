# مسودة إجابات نماذج Play Console — صحّح حروفك

## 1. تصنيف المحتوى (Content Rating Questionnaire)

- **الفئة**: تطبيق تعليمي (Education / Reference, Utility & Productivity حسب القائمة المتاحة — اختر "Education")
- العنف: لا يوجد
- المحتوى الجنسي: لا يوجد
- اللغة البذيئة: لا يوجد
- المخدرات/الكحول/التبغ: لا يوجد
- القمار: لا يوجد
- التواصل مع مستخدمين آخرين (Chat/UGC): لا يوجد — لا توجد ميزة تواصل بين المستخدمين
- مشاركة الموقع الجغرافي: لا
- عمليات شراء داخل التطبيق: **نعم** (الاشتراك المدفوع عبر Stripe)

النتيجة المتوقعة: تصنيف عمري منخفض جداً (3+ أو ALL حسب النظام المحلي).

## 2. الجمهور المستهدف وسياسة الأطفال (Target Audience & Content / Families Policy)

- **الفئة العمرية المستهدفة**: التطبيق **موجّه بشكل أساسي للأطفال** لكن **يُدار بالكامل من قبل ولي الأمر** (إنشاء الحساب، الدفع، الإعدادات كلها بيد البالغ).
- عند سؤال Google "هل هذا تطبيق موجّه للأطفال بشكل أساسي (Primarily child-directed)؟" — الإجابة الصادقة هنا **نعم**، لأن الفئة المستهدفة الفعلية (المستخدم النهائي) هم الأطفال دون سن 13، حتى لو كان الحساب يُدار من الوالدين.
- هذا يُفعّل تلقائياً متطلبات **Google Play Families Policy**، وأهمها:
  - يجب أن تكون كل الإعلانات (إن وُجدت) مصنّفة للأطفال — **لا ينطبق، لا توجد إعلانات في التطبيق حالياً**.
  - لا يجوز جمع بيانات تعريف شخصية من الطفل نفسه دون رضا الوالدين الموثّق (COPPA) — **متحقق: الحساب ينشئه الوالد فقط**.
  - يجب تفعيل "Data Safety" بدقة (القسم التالي).

## 3. نموذج أمان البيانات (Data Safety Form)

بناءً على `privacy.html` والكود الفعلي (Firebase Auth + Firestore + Stripe + ElevenLabs):

| نوع البيانات | تُجمع؟ | تُشارك مع طرف ثالث؟ | الغرض |
|---|---|---|---|
| البريد الإلكتروني (حساب الوالد) | نعم | لا (تُخزَّن في Firebase فقط) | إنشاء الحساب/تسجيل الدخول |
| كلمة السر | نعم (مشفّرة عبر Firebase Auth) | لا | المصادقة |
| اسم الطفل | نعم | لا | تخصيص التجربة |
| بيانات التقدّم التعليمي | نعم | لا | تتبّع تقدّم الطفل |
| معلومات الدفع (رقم البطاقة) | **لا** — لا تصل إلى خوادمنا إطلاقاً | تُعالَج بالكامل عبر Stripe مباشرة | الاشتراك المدفوع |
| بيانات الموقع الجغرافي | لا | — | — |
| هوية الجهاز/الإعلانات | لا | — | — |

- **هل يتم تشفير البيانات أثناء النقل؟** نعم (HTTPS عبر Firebase وStripe).
- **هل يمكن للمستخدم حذف بياناته؟** نعم (زر حذف الحساب داخل التطبيق).
- **هل جُمعت البيانات اختيارية أم إلزامية؟** إلزامية جزئياً (البريد وكلمة السر لإنشاء الحساب)، واختيارية جزئياً (اسم الطفل تجميلي).

## 4. رابط سياسة الخصوصية (يُطلب في أكثر من مكان)

```
https://archmohamedmerie-coder.github.io/horofi/privacy.html
```
