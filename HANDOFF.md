# ملف تسليم — صحّح حروفك

## 🟢 ابدأ من هنا — الحالة عند 02.09.2026 الساعة 00:30

**لا يوجد أي إجراء مطلوب الآن. ننتظر قرار آبل فقط.**

| المنصّة | الحالة |
|---|---|
| **أندرويد** | منشور للجمهور — versionCode 12 (1.9)، يتضمّن In-App Review |
| **iOS** | Build 1.0 (7) + الاشتراك + مجموعته → **«Warten auf Prüfung»** لدى آبل منذ 02.09.2026 00:21 |

**أول ما تفعله في جلسة جديدة**: افحص بريد `arch.mohamedmerie@gmail.com` بحثاً عن
رسالة من App Store Connect، أو افتح صفحة التقديم مباشرة:
`https://appstoreconnect.apple.com/apps/6795959824/distribution/reviewsubmissions/details/5b1f74c8-5302-44e4-a2f0-9a1dd8ade621`

**معرّفات يُحتاج إليها**:
- Apple App ID: `6795959824` | Team: `b1a02f07-20ac-465a-b552-526e4c2a74a9`
- Bundle ID: `com.horofi.app` | مجموعة TestFlight «Familie»: `d0a1da80-5f31-4218-b6e9-9f69f8354137`
- Firebase project: `horofi` | Codemagic appId: `6a673beaf1d3a42a0686a30b`,
  workflow `ios-testflight` (توكن الـAPI عند المستخدم، لا يُخزَّن هنا)
- آخر كوميت: `583d656` (iOS Build 7)

**قواعد عمل ثابتة في هذا المشروع** (تعلّمناها بثمن):
1. **لا تؤكّد نجاح أي حفظ في App Store Connect قبل إعادة تحميل الصفحة والتحقق
   من القيمة.** تعطُّل زر «Sichern» أو تغيّر عدّاد الأحرف ليس دليلاً — ضاع
   تعديل وصف EULA مرة بهذه الطريقة وسبّب رفضاً كاملاً.
2. **لا تكتفِ بمراجعة الكود — شغّل السيناريو فعلياً.** إصلاح تسريب اسم الطفل
   بدا صحيحاً ومرّ فحص الصياغة، لكن الاختبار الحيّ كشف أنه غير كافٍ.
3. **Codemagic يبني من GitHub لا من القرص المحلي** — أي تعديل بلا `git push`
   لا يدخل البناء إطلاقاً.
4. لضبط حقول React في App Store Connect استخدم
   `Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value').set`
   ثم `dispatchEvent(new Event('input',{bubbles:true}))`؛ و`form_input` يفشل صامتاً.

---

# سجل الجلسات (28 أغسطس – 2 سبتمبر 2026)

## ✅ (02.09.2026 — 00:21): أُرسل فعلياً لآبل — Build 7 + الرد + الفيديو

**تاريخ التقديم الجديد**: 2. Sept. 2026 um 00:21 Uhr — العناصر الثلاثة كلها
**"Warten auf Prüfung"**: الاشتراك، `iOS-App 1.0 (7)`، ومجموعة الاشتراك.

**ما أُرسل**:
- **Build 1.0 (7)** (كوميت `583d656` — يتضمّن وضع الضيف + إصلاح تسريب اسم الطفل)
- **ردّ إنجليزي** (2118 حرفاً) ظاهر في الخيط باسم Mohamed Merie بتاريخ 00:14،
  يشرح المخالفات الثلاث بتوقيتات الفيديو وخطوات تحقّق مباشرة للمراجع
- **الفيديو** `IOS VideoIOS Video1.mp4` (12.81MB) مرفوع في حقل **Anhang**
  ضمن "Informationen zur App-Prüfung" (رفعه المستخدم يدوياً — أداة الرفع
  الآلي محدودة بـ10MB)

**تصحيح مهم سجّلتُه على نفسي**: استنتجتُ خطأً أن الفيديو لا يُظهر الضغط على زر
الضيف، لأن التطبيق وصل للشاشة الرئيسية بطفل موجود بدل شاشة التهيئة. الصواب أن
**الخلل نفسه** (وراثة `horofiProfile`) هو ما جعل دخولاً حقيقياً كضيف يبدو
كدخول تلقائي. المستخدم صحّح لي ذلك، فحُذفت من الرد جملة كانت ستقول لآبل خطأً
إن التسجيل جرى على جهاز مسجَّل الدخول مسبقاً.

**مسار الإرسال الصحيح** (الزر يختلف حسب الحالة):
- حين تكون النسخة `Abgelehnt` → زر **«Erneut zur App-Prüfung übermitteln»**
  في صفحة التقديم؛ لكنه **يُعطَّل بعد إرسال ردّ**.
- الحل: من صفحة الإصدار اضغط **«Prüfung aktualisieren»** (تنقل الحالة إلى
  `Bereit zur Prüfung`)، عندها يعود زر إعادة الإرسال مفعّلاً في صفحة التقديم.
- ⚠️ صفحة الإصدار كانت تُحمّل الشريط الجانبي فقط (396 حرفاً) عدة مرات؛
  الحل إعادة التنقّل حتى يتجاوز المحتوى ~5800 حرف قبل قراءة الأزرار.

**المتبقّي**: انتظار قرار آبل فقط. لا إجراء مطلوب.


## 🔴 (01.09.2026): تسريب خصوصية عند تسجيل الخروج — أُصلح، Build 7

**كيف اكتُشف**: المستخدم لاحظ أن أخاه سجّل خروجاً لكن **بقيت تحية الطفل
السابق ("هاشم") ظاهرة**. لولا ملاحظته لأُرسل Build 6 بهذا الخلل.

**السببان (متراكبان، وكلاهما في `logoutUser()`)**:
1. الدالة تُصفّر `CURRENT_UID` و`CHILDREN` و`ACTIVE_CHILD_ID` **لكن ليس
   `PROFILE`** — فيبقى اسم الطفل السابق في الذاكرة ويعرضه `buildHome()`.
2. **الأخطر** (اكتُشف فقط لأن الإصلاح الأول اختُبر ولم يُفترض نجاحه):
   `saveProfile()` تكتب نسخة احتياطية في المفتاحين القديمين
   `horofiProfile` / `horofiProgress` وهما **غير مرتبطين بأي uid**؛
   و`loadChildren()` تحتوي مسار ترحيل يقرأ `horofiProfile` ويُنشئ منه طفلاً
   لأي حساب ليس لديه أطفال. النتيجة: **كل حساب جديد على الجهاز — حتى حساب
   بريد مختلف تماماً — كان يرث طفل من سبقه.** تسريب خصوصية حقيقي على جهاز
   مشترك، وموجود منذ ما قبل تعديلات هذه الجلسة.

**الإصلاح** (داخل `auth.signOut().then(...)`):
- تصفير `PROFILE` و`completedLetters`
- `localStorage.removeItem('horofiProfile')` و`horofiProgress`
  (نفس ما تفعله `deleteAccount()` أصلاً)
- تفريغ حقول شاشة التهيئة + `resetGenderPicker()`
- إعادة `homeGreeting` لنصّه الافتراضي

**التحقق الحيّ (من صفر، على Firebase الإنتاجي)**:
ضيف A أنشأ «زينب» وظهرت التحية ← خروج ← ضيف B (uid مختلف):
`screen=onboarding`، `PROFILE.name=""`، `CHILDREN=0`، التحية افتراضية،
والمفتاح القديم `horofiProfile` = `null`. حُذفت حسابات الاختبار ووثائقها.

**Build 7** = كوميت `583d656`، بناء Codemagic `6a973eebffa33bf40f40bbd3`
نجح بالكامل، رُفع 01.09 الساعة 23:13. **يُلغي Build 6.**

⚠️ **درس**: الإصلاح الأول بدا صحيحاً ومرّ فحص الصياغة، لكن الاختبار الحيّ
كشف أنه لا يكفي. لا يُكتفى بمراجعة الكود — يجب تشغيل السيناريو فعلياً.


## ✅ (01.09.2026): رفض مراجعة بشرية (3 مخالفات) — أُصلحت كلها، Build 6

**رسالة آبل** (31.08 لكن وصلت 01.09 الساعة 12:38): هذه أول **مراجعة بشرية
حقيقية** (Review Device: iPhone 17 Pro Max, Version reviewed: 1.0 (5)) — وليست
فحصاً آلياً كسابقتها. ثلاث مخالفات:

1. **Guideline 5.1.1(v)** — التطبيق يُجبر على التسجيل للوصول لميزات لا تعتمد
   على حساب.
2. **Guideline 2.1** — تعذّر تسجيل الدخول بالحساب التجريبي
   (`تمارا` / `#Start12#`).
3. **Guideline 3.1.2(c)** — نقص معلومات الاشتراك المتجدّد.

**التشخيص الفعلي بعد قراءة الكود** (لا تخمين):
- سبب (1): `auth.onAuthStateChanged` كانت البوابة الوحيدة — إن لم يوجد مستخدم
  Firebase تُنفَّذ `nav('login')` فوراً فلا يظهر أي محتوى.
- سبب (2): `doLogin()` تستخدم `signInWithEmailAndPassword` أي تتطلّب **بريداً
  إلكترونياً**؛ و"تمارا" اسم وليس بريداً فيرفضه Firebase قبل التحقق من كلمة
  السر. **الكود سليم — الخطأ في البيانات المُدخَلة في App Store Connect.**
- سبب (3): إعادة قراءة نص آبل بدقّة كشفت أن المطلوب معلومات **داخل التطبيق
  نفسه** (اسم الاشتراك + مدّته + سعره + رابطا سياسة الخصوصية وشروط الاستخدام)،
  وليس في وصف المتجر فقط. صفحة الاشتراك كانت تعرض الاسم والسعر فقط. لذلك
  تكرّرت ملاحظة EULA رغم تعديل وصف المتجر في 31.08.

**الحل المُنفَّذ — وضع الضيف عبر Firebase Anonymous Auth**
(اختير بعد مقارنة موثَّقة مع بديل "إلغاء Firebase كلياً" الذي رُفض لأن
`loadSubscription()` تقرأ `horofiSubscribed` من localStorage **قبل** التحقق من
`CURRENT_UID` — فمشترك سابق خرج من حسابه كان سيحصل على وصول مجاني):
- `firestore.rules` تعمل بلا تعديل (`request.auth != null` يتحقّق للضيف).
- `CURRENT_UID` يبقى مضبوطاً ⇒ الاشتراك ومزامنة الأطخال وStripe/StoreKit
  ومفاتيح localStorage كلها تعمل كما هي.

**تعديلات الكود في `horofi-v11-9-29.html`**:
- زر بارز «🎈 ابدأ اللعب بدون تسجيل» + `continueAsGuest()` (`signInAnonymously`)
- **`doRegister()` تستخدم `linkWithCredential` إن كان المستخدم ضيفاً** بدل
  `createUserWithEmailAndPassword` — الأولى **تحتفظ بنفس الـuid**، والثانية
  كانت ستُنشئ uid جديداً فيفقد المستخدم اشتراكه المدفوع وتقدّم أطفاله. هذه
  أخطر نقطة في التعديل كله.
- `upgradeGuestAccount()` + زر «أنشئ حساباً لحفظ التقدّم» في صفحة الحساب
- زر رجوع في شاشة الدخول يظهر تلقائياً من `nav()` حين يكون المستخدم مسجَّلاً
  (بدونه يعلق الضيف على iOS — لا زر رجوع في الجهاز)
- تحذير صريح عند خروج الضيف (حسابه غير قابل للاستعادة)
- `openTermsOfUse()` + `TERMS_OF_USE_URL` (شروط آبل القياسية)
- صفحة الاشتراك: اسم الاشتراك + «مدّته شهر واحد» + بند التجديد التلقائي +
  رابطا سياسة الخصوصية وشروط الاستخدام

**Firebase Console**: فُعِّل مزوّد **Anonymous** (كان غير موجود إطلاقاً).
**تُرك خيار «Automatische Bereinigung» مُطفأً عمداً** — لأنه يحذف حسابات
الضيوف بعد 30 يوماً، ومفاتيح localStorage مرتبطة بالـuid عبر `_chKey`، فكان
سيمحو تقدّم الأطفال بصمت.

**الاختبار الحيّ (خادم محلي على localhost، Firebase الإنتاجي الحقيقي)**:
| ما اختُبر | النتيجة |
|---|---|
| صياغة JS لكل السكربتات | ✅ تُحلَّل بلا أخطاء (node vm.Script) |
| ضغط زر الضيف | ✅ `isAnonymous:true` + uid حقيقي + `CURRENT_UID` مضبوط |
| التهيئة وإنشاء طفلة | ✅ وصل للشاشة الرئيسية، الطفلة محفوظة |
| كتابة Firestore كضيف | ✅ نجحت تحت القواعد الإنتاجية، و`subscribed` بقي محمياً |
| صفحة الحساب للضيف | ✅ تعرض «ضيف» + زر الترقية، وتُخفي «تغيير كلمة السر» |
| صفحة الاشتراك | ✅ الاسم والمدة والتجديد والسعر والرابطان كلها ظاهرة |
| **ترقية الضيف** | ✅ **uid لم يتغيّر إطلاقاً**، `isAnonymous:false`، الطفلة سليمة |
| تنظيف بيانات الاختبار | ✅ حُذف حساب الاختبار ووثيقته من Firestore |

**ملاحظة بنيوية مهمة**: `android-app/ios/.../public/index.html` و
`android-app/www/index.html` قديمان محلياً، **وهذا لا يؤثر إطلاقاً** — لأن
`codemagic.yaml:22-25` ينفّذ `cp ../horofi-v11-9-29.html www/index.html` ثم
`npx cap sync ios` أثناء البناء. أي أن iOS يُبنى دائماً من الملف المصدر في
جذر المستودع. يكفي رفع `horofi-v11-9-29.html` وحده.

**أندرويد مؤجَّل بقرار المستخدم** — منشور وفعّال، وسيُحدَّث لاحقاً بنفس الكود.

### ⚠️ اكتشاف مهم: تعديل وصف المتجر في 31.08 لم يُحفَظ فعلياً

عند التحقق بعد إعادة تحميل كاملة (01.09) تبيّن أن حقل `description` عاد إلى
**843 حرفاً بلا أي رابط EULA** — أي أن تعديل 31.08 **لم يصل الخادم إطلاقاً**،
رغم أن عدّاد الأحرف في الواجهة كان قد تغيّر وقتها من 3.157 إلى 3.052 (وهو عدّاد
"المتبقّي"، فتغيّره يعكس الحالة المحلية فقط لا الحفظ). **هذا هو سبب تكرار آبل
لملاحظة 3.1.2 في المراجعة البشرية.**

**الدرس العملي لكل تعديل قادم في App Store Connect**:
1. لا تعتمد على `form_input` ولا على تغيّر العدّاد ولا على تعطُّل زر "Sichern"
   كدليل على الحفظ — كلها تعكس حالة الواجهة فقط.
2. اضبط الحقل بالطريقة المتوافقة مع React:
   `Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value').set`
   ثم `dispatchEvent(new Event('input',{bubbles:true}))`.
3. **أعد تحميل الصفحة كاملةً من الخادم وتحقّق من القيمة** قبل اعتبار الحفظ ناجحاً.

بعد تطبيق ذلك تأكّد ثبات الثلاثة معاً بعد إعادة تحميل: `description` = 948 حرفاً
ويحوي `stdeula` ✅، `demoAccountRequired` = false ✅، `notes` = 1386 حرفاً ✅.

### الحالة عند نهاية جلسة 01.09 — كل شيء جاهز، بانتظار ضغطة الإرسال فقط
- Codemagic build `6a9701d9889be88e59a2859d` **نجح بالكامل** على الكوميت
  `fa086c7`؛ رُفع Build 6 إلى آبل (Sep 1, 6:57 PM)
- **التوافق التصديري لـBuild 6**: أُجيب بـ«Keinen der oben genannten
  Algorithmen» (لا شيء مما ذُكر) — وهو الصحيح تقنياً (التطبيق يستخدم HTTPS من
  نظام آبل فقط ولا يحزم أي مكتبة تشفير)، **ومتسق مع Build 5** الذي تحقّقتُ من
  بياناته الوصفية فوجدت «Verschlüsselung erfordert Unterlagen: Nein».
  Build 6 صار «Bereit zur Übermittlung».
- **Build 6 مُرفق بالتقديم** بدل Build 5 (تحقّق بعد إعادة تحميل: صف
  Build = `61.0Nein`)، ولقطات الشاشة السبع سليمة لم تُمس
- الوصف 948 حرفاً مع رابط EULA ✅، الملاحظات 1386 حرفاً ✅،
  «Anmeldung erforderlich» مُلغاة ✅ — كلها مؤكَّدة بعد إعادة تحميل
- التقديم `5b1f74c8` يضم العناصر الثلاثة، وبند App-Version صار **1.0 (6)**
- **المتبقّي الوحيد**: ضغط «Erneut zur App-Prüfung übermitteln» — مؤجَّل
  بانتظار موافقة المستخدم الصريحة (فعل رسمي تجاه آبل)
- ملاحظة: آبل طلبت أيضاً الردّ على رسالتها بتسجيل شاشة يؤكّد وجود المعلومات
  («Auf App-Prüfung antworten») — لم يُرسَل أي ردّ بعد
- **Build 6 أُضيف لمجموعة TestFlight الخارجية «Familie»** وحالته «Im Test»
  فوراً بلا انتظار Beta App Review (المجموعة صارت 4 Builds). أي أن أخا المستخدم
  يستطيع تحديث التطبيق عبر TestFlight الآن. ملاحظات الاختبار بالعربية تطلب منه
  تجربة زر الضيف ثم فتح صفحة الاشتراك.
  ⚠️ عند الكتابة في حقل TestFlight (`localizations[ar-sa].whatsNew`) **تسقط
  المسافات** مع `computer.type`؛ الحل الذي نجح:
  `document.execCommand('insertText', false, txt)` بعد `focus()`.

### فيديو أخي المستخدم — فُحص فعلياً إطاراً بإطار (01.09)
الملف `IOS Video.mp4` (8.8MB، **46.76 ثانية**، 384×848) في جذر المشروع.
لا يوجد ffmpeg على الجهاز، فاستُخرجت الإطارات عبر صفحة HTML مؤقتة تُحمّل
الفيديو وتُنفّذ `seek` ثم `drawImage` على canvas، وتُخدَم عبر
`npx http-server` (نفس الأصل ضروري وإلا تتلوّث الـcanvas). حُذفت الصفحة بعدها.

**ما يُثبته الفيديو (مؤكَّد بصرياً)**:
- Build 6 مثبَّت فعلاً (نص المدة/التجديد الجديد ظاهر)
- ث 11-12: اسم الاشتراك + «مدّته شهر واحد» + بند التجديد + 2.99 يورو/شهر
- ث 13-19: **ضُغط رابط سياسة الخصوصية وفُتحت الصفحة فعلاً**
- ث 21-24: **ضُغط رابط شروط الاستخدام وفُتحت EULA من apple.com فعلاً**
  (هذا يحقّق شرط آبل «functional links» لا مجرد عرضها)
- ث 33-46: شبكة الحروف الـ28 ولعب جولة كاملة

**الناقص**: الفيديو يبدأ **داخل التطبيق أصلاً** (شاشة «أهلاً يا هاشم») —
لا شاشة دخول ولا زر ضيف. أي أن **مخالفة 5.1.1 غير مُثبَتة** فيه.
مطلوب مقطع ثانٍ ~20 ثانية: خروج ← شاشة الدخول ← الزر الأصفر ← دخول بلا بيانات.

### فحص وقائي: التطبيق ليس في فئة الأطفال
تحقّقتُ برمجياً من `primaryCategory` في App Store Connect: القيمة **«Bildung»
(تعليم)**، وخيار «Kinder» غير موجود أصلاً في القائمة؛ التصنيف العمري 4+.
⇒ **لا يلزم parental gate** قبل الروابط الخارجية (الخصوصية/الشروط/Stripe)،
وهي تُفتح حالياً مباشرة بلا بوابة. لو كان التطبيق في فئة الأطفال لكان هذا سبب
رفض إضافياً.

### مسودة الرد على آبل — محفوظة وغير مُرسَلة
كُتب ردّ إنجليزي (1629 حرفاً) يشرح المخالفات الثلاث ومواضع الدليل بالتوقيتات،
وحُفظ عبر **«Entwurf sichern»** (لا «Antworten»). يظهر الآن في خيط الرسائل
باسم Mohamed Merie مع «Entwurf fortsetzen / Entwurf löschen».
⚠️ **لم يُرسَل** — بانتظار المقطع الثاني ليُرفق معه (نافذة الرد تدعم
«Datei anhängen»).



## ✅ (31.08.2026): رفض آلي أول بسبب رابط EULA مفقود — أُصلح وأُعيد الإرسال

**الرسالة**: وصل بريد من App Store Connect الساعة 5:15 صباحاً (31.08.2026) بعنوان
"There's an issue with your صحّح حروفك (iOS) submission." — تحقّقت من فتحه
وقراءته كاملاً بنفسي (وليس فقط من عنوان الإشعار). المحتوى: رفض آلي (3.1.2
Business: Payments - Subscriptions) لأن التطبيق يقدّم اشتراكاً بتجديد تلقائي
لكن وصف التطبيق في App Store لا يحتوي رابطاً فعّالاً لشروط الاستخدام (EULA).

**الإصلاح**:
1. قرأت القيمة الحالية لحقل `description` (textarea، id="description"، 843
   حرفاً) عبر JS مباشرة للتأكد من محتواه الدقيق قبل التعديل
2. أضفت سطراً جديداً في نهاية الوصف عبر أداة `form_input` (وليس حقناً مباشراً
   بـJS، لضمان تحديث حالة React بشكل صحيح):
   `شروط الاستخدام (Terms of Use / EULA): https://www.apple.com/legal/internet-services/itunes/dev/stdeula/`
   (رابط شروط استخدام آبل القياسية، بما أن التطبيق لا يستخدم EULA مخصّصة)
3. تحقّقت حيّاً أن الطول صار 948 حرفاً وأن عدّاد الأحرف في الصفحة تغيّر من
   "3.157" إلى "3.052" (فرق 105 حرفاً يطابق تماماً طول السطر المُضاف) قبل
   الضغط على "Sichern"
4. من صفحة "App-Prüfung" → تفاصيل التقديم
   (`reviewsubmissions/details/5b1f74c8-5302-44e4-a2f0-9a1dd8ade621` — نفس
   Submission ID المذكور في رسالة آبل)، ضغطت "Erneut zur App-Prüfung
   übermitteln" (إعادة الإرسال)

**التحقّق النهائي (حيّ)**: حالة التقديم أصبحت "Warten auf Prüfung" لكل
العناصر الثلاثة (النسخة + الاشتراك + مجموعة الاشتراك)، بتاريخ تقديم جديد:
31. Aug. 2026 um 07:41 Uhr. التطبيق الآن في طابور المراجعة مجدداً بعد إصلاح
السبب الوحيد للرفض.


## ✅✅ (29.08.2026): المشكلة العالقة الأساسية انتهت — التطبيق + الاشتراك أُرسلا فعلياً لمراجعة آبل

**هذا هو الحل النهائي للمشكلة التي كانت السبب الأصلي في توقّف المستخدم بالكامل**
(عدم ظهور زر "+" لإرفاق Build جديد بسبب أن Build 2 القديم المرفوض ظلّ مرتبطاً
بالإصدار). تم تنفيذه بالكامل والتحقّق منه حيّاً خطوة بخطوة:

1. **حذف Build 2 القديم**: في صفحة الإصدار
   (`.../distribution/ios/version/inflight`)، قسم "Build" كان يعرض جدولاً
   بـBuild 2 فقط ولا يوجد زر إضافة. عُثر على زر "Löschen" مخفي (hover-only،
   width:0) داخل صف Build 2 عبر فحص DOM مباشرة، وضُغط عليه عبر `find`+`ref`
   (وليس إحداثيات شاشة، لأن الإحداثيات المحسوبة من `getBoundingClientRect`
   كانت تُخطئ الهدف بسبب نسبة تحويل غير ثابتة بين بكسلات CSS وبكسلات لقطة
   الشاشة في هذه الجلسة). **بعد الحذف مباشرة**، تحقّقتُ عبر `get_page_text`
   أن القسم تحوّل فعلاً من جدول Build 2 إلى زر **"Build hinzufügen"** — وهو
   بالضبط الزر المفقود الذي كان سبب التوقّف.

2. **إرفاق Build 5**: عبر "Build hinzufügen"، اخترتُ Build 5 من قائمة راديو
   (تحقّقتُ عبر JS من قيمة الراديو المُحدَّد فعلياً قبل التأكيد، لتفادي تكرار
   خطأ إرفاق Build 2 بالغلط)، ثم "Fertig". تحقّقتُ عبر `get_page_text` أن قسم
   Build أصبح يعرض "5 / 1.0 / NEIN".

3. **حفظ**: زر "Sichern" غيّر حالة الإصدار من "Vom Entwickler abgelehnt" إلى
   "In Vorbereitung zur Übermittlung" — تحقّقتُ من ذلك حيّاً.

4. **إرفاق الاشتراك الأول**: بطلب صريح من المستخدم ("اختبر اذا اول اشتراك
   سليم بلا مشاكل ثم ارفقه مع النسخة"), فحصتُ صفحة الاشتراك
   (`اشتراك شهري 2.99 يورو`, `com.Horofi.monthly2eur`, شهر واحد) بالكامل:
   الاسم المرجعي، معرّف المنتج، التوفّر (كل الدول)، السعر، الترجمة العربية
   (اسم + وصف)، وصورة مراجعة (Screenshot) موجودة فعلياً. لا خطأ ظاهر.
   اكتشفتُ أن نظام آبل الجديد يستخدم "مسودة تقديم" (Übermittlungsentwurf)
   موحّدة يمكن أن تضمّ عدة عناصر (نسخة تطبيق + اشتراك فردي + **مجموعة
   الاشتراك نفسها** — الثلاثة مطلوبون معاً، ليس الاشتراك الفردي وحده). أضفتُ
   الثلاثة للمسودة نفسها من صفحاتهم المختلفة (زر "Zur Prüfung hinzufügen" ثم
   اختيار المسودة القائمة بدل "Neue Übermittlung erstellen" في كل مرة).

5. **الإرسال النهائي**: بعد أن اختفى كل تحذير وأصبح زر "Zur Prüfung
   übermitteln" مفعّلاً (أزرق)، طلبتُ تأكيداً صريحاً من المستخدم قبل الضغط
   (لأنه فعل رسمي غير قابل للتراجع بسهولة) — وافق بـ"نعم". ضُغط الزر، ظهرت
   حالة "Wird versendet" (جاري الإرسال)، ثم **تحقّقتُ حيّاً بعدها مباشرة**
   عبر `get_page_text` في صفحتين منفصلتين:
   - صفحة الاشتراك: الحالة أصبحت **"Warten auf Prüfung"** (بانتظار المراجعة)
   - صفحة نسخة iOS: الحالة أصبحت **"1.0 Warten auf Prüfung"**، مع ملاحظة
     رسمية من آبل: "يمكنك تعديل بعض المعلومات أثناء حالة الانتظار؛ لتقديم
     Build جديد يجب أولاً سحب هذه النسخة من المراجعة."

**الخلاصة**: التطبيق (Build 5) والاشتراك الأول معاً **في طابور مراجعة آبل
الفعلي الآن** (29.08.2026)، بعد أسابيع من التوقّف بسبب Build 2 القديم العالق.
لا حاجة لأي إجراء إضافي إلا انتظار قرار آبل (قبول/رفض/طلب توضيح) ومتابعته عبر
البريد أو App Store Connect مباشرة.

## ✅ (28.08.2026): iOS Build 5 أُضيف فعلياً لمجموعة TestFlight الخارجية "Familie"

**تصحيح مهم**: المحاولة الأولى لإضافة Build 5 لمجموعة "Familie" (في نفس الجلسة
السابقة، قبل الملخّص) **لم تنجح فعلياً** — رغم أنني ظننت وقتها أنني ضغطت "Zur
Prüfung übermitteln" بنجاح. لدى التحقق الحيّ لاحقاً (`get_page_text` على صفحة
builds الخاصة بالمجموعة)، تبيّن أن المجموعة كانت لا تزال تعرض Builds (2) فقط
(Build 3 وBuild 2) — Build 5 لم يكن مُدرَجاً إطلاقاً. هذا تطبيق مباشر لقاعدة
"لا تؤكّد قبل التحقق الفعلي".

**إعادة المحاولة نجحت هذه المرة، مع مشكلة تقنية تم حلّها**:
- عند فتح نافذة "Was soll getestet werden؟" وكتابة ملاحظات الاختبار، النقر على
  إحداثيات الشاشة (screenshot pixels) لم يكن يُركّز فعلياً على حقل النص
  الفعلي (`contenteditable` بمعرّف `localizations[ar-sa].whatsNew`) — تبيّن أن
  نسبة تحويل الإحداثيات بين `getBoundingClientRect()` (بكسلات CSS، عرض نافذة
  1851) وبكسلات لقطة الشاشة (1552) لم تكن ثابتة كما افتُرض سابقاً في هذه
  الجلسة تحديداً، فالنقر بالإحداثيات المحسوبة أخطأ الهدف عدة مرات متتالية
  (تحقّقتُ من ذلك عبر `document.activeElement` بعد كل نقرة، فوجدته `DIALOG`
  وليس الحقل).
- الحل: تحديد العنصر الصحيح عبر JS (`document.getElementById(...)`)،
  استدعاء `.focus()` عليه مباشرة، ثم التحقّق من أن `document.activeElement`
  أصبح فعلاً هو الحقل المطلوب، وبعدها فقط استخدام أداة الكتابة الحقيقية
  (`computer.type`) لإدخال النص عبر أحداث لوحة مفاتيح حقيقية — وليس حقن النص
  عبر JS مباشرة. تحقّقتُ من نجاح الكتابة عبر قراءة `textContent` الفعلي للحقل
  بعد الكتابة.
- بعد ذلك: النقر على زر "Zur Prüfung übermitteln" عبر `find` + `ref` (وليس
  إحداثيات) نجح.

**التحقّق النهائي (حيّ، عبر `get_page_text` على صفحة
`.../testflight/groups/d0a1da80-5f31-4218-b6e9-9f69f8354137/builds`)**:
مجموعة "Familie" أصبحت تعرض **"Builds (3)"**، بإضافة:
```
Build 1.0 (5) — Im Test — Läuft in 90 Tagen ab
```
أي أن Build 5 أصبح **"قيد الاختبار" (Im Test) فعلياً ومتاحاً للتثبيت الآن** —
لم يتطلّب انتظار مراجعة Beta App Review إضافية هذه المرة (على الأرجح لأن نفس
رقم الإصدار 1.0 سبق اعتماده لهذه المجموعة مع Build 2 وBuild 3).

**الخطوة التالية**: الطلب من أحد أفراد العائلة (صاحب iPhone 17 Pro Max، عضو
مجموعة "Familie") فتح تطبيق TestFlight وتثبيت/تحديث Build 5 وتجربة الشاشات
الأساسية للتأكد من عدم وجود انهيار (crash) قبل إلحاق هذا الـBuild بمراجعة آبل
الرئيسية المُعلَّقة أصلاً (`163ac6d2-34d8-4d0c-ae68-6080c7b25db2`).

## ✅ (28.08.2026): versionCode 12 (1.9) أُرسل فعلياً لمراجعة جوجل — يتضمّن In-App Review

**ما حدث بالتفصيل**: رفعنا `versionCode` إلى 12 و`versionName` إلى "1.9"
(`android-app/android/app/build.gradle`)، بنينا حزمة `bundleRelease` محلياً
(**BUILD SUCCESSFUL**، ~128 ميجابايت)، وتحقّقنا من الـmanifest الفعلي المُعالَج
للحزمة — `versionCode="12"`, `versionName="1.9"`, `minSdkVersion="23"` — قبل
الرفع، وليس بعده فقط.

**اختبرنا ميزة In-App Review حيّاً على محاكي Pixel_8_stable قبل الرفع**:
- الإضافة (`@capacitor-community/in-app-review`) مُسجَّلة بنجاح، والدالة
  `maybeRequestNativeReview()` (مُضافة في `pmSendAndClose()`) تُستدعى بلا أخطاء
- سجلات `logcat` أكّدت اتصال PlayCore الحقيقي واكتمال العملية تقنياً
  (`ServiceConnectionImpl.onServiceConnected` → `Request review flow finished`)
- **لكن لم تظهر أي نافذة مرئية فعلياً** — هذا **متوقَّع وليس عطلاً**: واجهة
  Google الرسمية لا تعرض النافذة عند تثبيت مباشر عبر `adb install` (تجاوز
  المتجر)، بصرف النظر عن كون الجهاز محاكياً أو هاتفاً حقيقياً. الاختبار الحقيقي
  لظهور النافذة يتطلّب تثبيتاً فعلياً عبر Play Store (مسار Internal Testing
  مثلاً) بحساب مختبِر مسجَّل — لم نفعل هذا بعد.
- تحقّقنا أيضاً حيّاً أن نموذج التقييم الخاص (Formspree) لا يزال يعمل بلا أي
  تعارض مع الكود الجديد (طلب مباشر عبر CDP أعاد `status=200 ok=true`).

**الملف كان جاهزاً هنا** (128MB، تجاوز حد أداة الرفع الآلي 10MB، فرُفع يدوياً من
المستخدم):
```
android-app/android/app/build/outputs/bundle/release/app-release.aab
```

**رُفع فعلياً عبر Play Console** (يدوياً من المستخدم، بمرافقتي خطوة بخطوة):
تحذير واحد غير حاسم فقط (نفس تحذير ملف Deobfuscation المعروف من قبل)، لا تغيير
في عدد الأجهزة المدعومة (13,334 هاتف، إلخ)، ملاحظات الإصدار بالعربية ظهرت
بشكل صحيح، 100% طرح كامل، "In allen Zielländern verfügbar" (كل الدول). **أُرسل
فعلياً للمراجعة** عبر زر "X Änderung(en) zur Überprüfung einreichen" في صفحة
"Veröffentlichungen – Übersicht" (نفس الخطوة الحاسمة الموثَّقة سابقاً التي
فاتت في versionCode 10).

**الخطوة التالية**: نفس المعالجة لـiOS (بناء 5 عبر Codemagic، فحص حيّ سريع عبر
TestFlight، ثم إلحاقه بمراجعة آبل المُعلَّقة أصلاً).

## 🔴 اكتشاف مهم (28.08.2026): الفرع main المحلي كان 8 commits متقدّماً على GitHub — لم يُدفَع أبداً

**أول محاولة بناء iOS 5 فشلت**: Codemagic بنى commit قديم (`36da014`، 14.08) بدل
أحدث كود — لأن `git push` لم يُنفَّذ **إطلاقاً طوال هذه الجلسة ولا الجلسات
السابقة** (`git status` أظهر "ahead of origin/main by 8 commits"). النتيجة:
البناء حمل `versionCode: 4` القديم، وفشل الرفع لـApp Store Connect بخطأ صريح:
**"The bundle version must be higher than the previously uploaded version: '4'"**
(آبل يرفض رفع نفس رقم البناء مرتين). البناء والتوقيع نفسهما نجحا بلا مشاكل —
المشكلة فقط في مصدر Git القديم.

**الدرس المهم**: بناء أندرويد محلي (`gradlew`) لا يتأثر بهذا لأنه يقرأ الملفات
المحلية مباشرة. **لكن Codemagic (iOS) يبني حصراً من GitHub** — أي تعديل محلي لن
يظهر في بناء iOS القادم إلا بعد `git push` صريح. **يجب دائماً التحقّق من
`git status` (ahead of origin؟) قبل أي بناء Codemagic مستقبلي.**

**الإصلاح**: `git push origin main` (دفع 9 commits دفعة واحدة، بما فيها ميزة
In-App Review ومعظم عمل الجلسات الأخيرة التي لم تُدفَع من قبل). أُعيد تشغيل
بناء Codemagic فوراً بعدها.

## ✅ (28.08.2026): iOS Build 5 بُني ورُفع بنجاح كامل لـApp Store Connect

بعد إصلاح مشكلة الدفع أعلاه، البناء الثاني نجح بالكامل:
- Commit صحيح مؤكَّد: `4e62f3f` (كل شيء حتى الآن، بما فيه In-App Review)
- **`versionCode: "5"`** مؤكَّد من الملف الناتج (App.ipa) نفسه
- كل خطوات البناء نجحت **بما فيها "Publishing"** (كانت هذه الخطوة التي فشلت في
  المحاولة الأولى)
- حالة معالجة آبل بعد الرفع: **"pending"** (طبيعي، يستغرق دقائق قبل التوفّر على
  TestFlight)

**الخطوة التالية**: انتظار اكتمال معالجة آبل، ثم فحص حيّ سريع عبر TestFlight
(تثبيت + فتح + تأكّد من عدم التعطّل، ومحاولة رؤية ميزة In-App Review) — **قبل**
إلحاق هذا البناء بمراجعة آبل النهائية المُعلَّقة (البند 6 أعلاه، عقدة زر "+").

> نصيحة: اطلب من كلود قراءة هذا الملف أولاً (`Read HANDOFF.md`) بدل إعادة شرح كل
> شيء. احذف الأقسام القديمة تدريجياً كي يبقى مختصراً ومفيداً.

## ✅ أُصلح (23.08.2026): بطاقة خصوصية التطبيق كانت خاطئة تماماً — "لا يُجمَع أي بيانات" بينما التطبيق يجمع 5 أنواع فعلياً

**اكتُشفت بالصدفة أثناء فحص روتيني**: صفحة **App-Datenschutz** في App Store Connect
كانت تعرض "Keine Daten erfasst — الا نجمع أي بيانات من هذا التطبيق" رغم أن الكود
الفعلي (`horofi-v11-9-29.html` + Firebase) يجمع بوضوح: بريد الحساب (Firebase Auth)،
بيانات الأطفال (الاسم/التقدّم/الشارات عبر Firestore)، معرّف المستخدم (UID)، سجل
المشتريات (اشتراك StoreKit)، وتقارير الأعطال (Crashlytics). هذا التضارب من أشهر
أسباب رفض/إزالة تطبيقات آبل (Guideline 5.1.1) — **مستقل تماماً عن عقدة الـBuild**،
كان سيبقى مشكلة حتى لو حُلّت عقدة iOS غداً.

**الإصلاح**: عبر متصفح Chrome المتصل (Claude in Chrome)، أعدنا تعبئة استبيان
"Datentypen" بدقة — 5 فئات فقط، كل واحدة بالغرض والربط بالهوية الصحيحين:
- **E-Mail-Adresse**: App-Funktionalität، مرتبط بالهوية
- **Sonstige Benutzerinhalte** (بيانات الأطفال): App-Funktionalität، مرتبط بالهوية
- **Benutzer-ID**: App-Funktionalität، مرتبط بالهوية
- **Käufe** (سجل الاشتراك): App-Funktionalität، مرتبط بالهوية
- **Crash-Daten**: App-Funktionalität، **غير** مرتبط بالهوية (لا `setUserId` في الكود)

لا شيء منها مُصنَّف كـ"Tracking". **نُشرت التغييرات فعلياً** (البطاقة العامة على
المتجر تعكسها الآن). فئات لم تُحدَّد عمداً لأنها غير مستخدمة فعلياً: Name (لا اسم
حقيقي للحساب)، Geräte-ID/Analyse (لا Firebase Analytics في iOS — فقط Crashlytics،
تحقّقنا من `Podfile`). تحقّقات إضافية أثناء نفس الجلسة: الفئة "Bildung" (ليست Kids
Category رسمياً)، تصنيف العمر 4+ سليم، اتفاقية آبل القياسية (EULA) تلقائية كافية،
حالة "تاجر" DSA مُسجَّلة، Restore Purchases موجود في الكود — كلها بلا مشاكل.

## تحقّق (23.08.2026): لا جديد في عقدة iOS — تحقّقنا حيّاً عبر متصفح Chrome المتصل بحساباتك

**تحقّقنا مباشرة عبر إضافة Claude in Chrome** (لا نسخ/لصق يدوي من المستخدم) من ثلاثة
أماكن:

1. **بوابة developer.apple.com/contact** → صفحة الحالة `20000137770756`: لا يوجد
   ردّ ظاهر في البوابة نفسها أصلاً — فقط تاريخ الإرسال الأصلي (14.08.2026) وزر
   "الحصول على مساعدة إضافية" الذي يفتح مراسلة **جديدة**، وليس عرضاً لسجل محادثة.
   البوابة لا تعرض الردود كمحادثة داخلية إطلاقاً.
2. **Gmail (`arch.mohamedmerie@gmail.com`)**: بحث كامل في الوارد + Spam عن أي بريد
   من Apple بعد 14.08.2026. **لا يوجد أي ردّ فعلي على التذكرة** — فقط رسالة التأكيد
   الآلية عند الإرسال (نفس يوم 14.08، "سنعاود التواصل معك بأقرب وقت ممكن") ورسائل
   Apple الروتينية غير المتعلقة (تجديد اتفاقية المطوّرين بتاريخ 18.08). لا شيء في
   Spam.
3. **App Store Connect → iOS-App Version 1.0 → Build**: لا يزال يعرض **البناء 2**
   فقط (القديم من دورة الرفض الأولى)، **لا يوجد زر "+"** بجانب عنوان "Build" لتغييره
   — نفس القفل الموثَّق في البند 6 أدناه بلا أي تغيير. قسم "Abos" أيضاً بلا تغيير
   (نفس مجموعة الاشتراك، نفس رسالة "أول اشتراك يجب أن يُرسَل مع إصدار تطبيق جديد").

**الخلاصة**: 9 أيام منذ إرسال التذكرة بلا أي ردّ من Apple Support، ولا تغيّر تقني
في واجهة App Store Connect نفسها. لا فائدة من تكرار هذا التحقّق يومياً — الحالة لا
تتغيّر إلا عند وصول ردّ فعلي من آبل (عبر البريد الإلكتروني، ليس البوابة).

**✅ رسالة متابعة أُرسلت فعلياً (23.08.2026)** عبر نفس التذكرة `20000137770756`
(نموذج "Kontaktoptionen → E-Mail" المرتبط بها، وليس تذكرة جديدة منفصلة): تلخّص
عقدة زر "+" الغائب في Build، وتطلب تحديث حالة أو حلاً بديلاً. تأكّد الإرسال عبر
رسالة "Vielen Dank für Ihre Nachricht" في الواجهة.

**✅ ردّ فعلي وصل من آبل (23.08.2026، الساعة 17:20)** — أول ردّ بشري منذ 14.08:
**"Sal"، مستشار أول (Senior Advisor) في Developer Support**، عبر بريد
`appreview@apple.com`. الردّ كان **عاماً/قياسياً** (يقول التطبيق في حالة "Ready for
Review" ولم يُرسَل بعد، ويطلب رفع بناء) ولم يعالج عقدة زر "+" تحديداً. عرض أيضاً:
مراسلة عبر قسم Reply في رسالة App Review، أو حجز **مكالمة فيديو 30 دقيقة** عبر
Meet with Apple، أو مكالمة مباشرة عبر
[developer.apple.com/distribute/app-review](https://developer.apple.com/distribute/app-review/).

**🔍 اكتشاف مهم**: التذكرة الأصلية (14.08، كتبها المستخدم بنفسه قبل هذه الجلسة)
كانت تحتوي **رقم Apple-ID خاطئ**: `6797636944` — بحثنا في كامل البريد ولم يظهر
هذا الرقم في أي مكان آخر إطلاقاً، مؤكِّدين أنه خطأ كتابي بسيط لا يمثّل أي حساب أو
تطبيق حقيقي. الرقم **الصحيح المؤكَّد مباشرة من App-Informationen** هو
`6795959824`. هذا على الأرجح فسّر جزئياً عمومية ردّ Sal.

**✅ ردّ تصحيحي أُرسل مباشرة لـSal عبر Gmail (23.08.2026، 17:57)** — رد داخل نفس
الخيط (وليس تذكرة جديدة) يوضّح: تصحيح رقم الـApple-ID، أن المشكلة ليست "لم يُرسَل
بعد" بل غياب زر "+" الفعلي، طلب صريح بمراجعة الرقم الصحيح، وتفضيل المراسلة
الكتابية على المكالمة (بسبب حاجز اللغة). **بانتظار الردّ الآن.**

## تحقّق (27.08.2026): 4 أيام صمت من Sal — أُرسلت رسالة إضافية عبر قناة App Review الرسمية

**تحقّقنا مجدداً بالكامل**: Gmail (الوارد + Spam) لا يوجد ردّ جديد منذ 23.08.2026
17:57. App Store Connect لا تغيير — لا يزال البناء 2 فقط، لا زر "+".

**✅ اكتُشفت قناة رسمية إضافية داخل App Store Connect نفسه** (منفصلة عن بريد
Developer Support/Sal): `Vertrieb → App-Prüfung → الإرسال (3 أغسطس، الحالة "Nicht
behobene Probleme") → قسم "Nachrichten" → زر "Auf App-Prüfung antworten"`. هذه
القناة مرتبطة مباشرة بسجل الإرسال المُعلَّق نفسه
(`163ac6d2-34d8-4d0c-ae68-6080c7b25db2`) وتصل لفريق **App Review** مباشرة، وليس
الدعم العام — هي نفس القناة التي أشار إليها Sal ("Reply section in the App Review
message") لكنها أدق وأكثر ارتباطاً بالحالة الفعلية.

**أُرسلت رسالة رابعة في هذا الخيط (27.08.2026، 16:24)** تشرح نفس عقدة زر "+"،
وتذكر رقم تذكرة Sal (`20000137770756`) للربط بين القناتين. **بانتظار الردّ على
القناتين معاً الآن (Developer Support + App Review) — لا فائدة من تكرار التحقّق
يومياً، فقط عند وصول إشعار فعلي.**

## 🎉 (22.08.2026): أندرويد منشور فعلياً للجمهور العام — أول نشر حقيقي

تحقّقنا من صفحة **Produktion → Releases** في Play Console مباشرة: الإصدار
**11 (1.8)** حالته **"✅ Bei Google Play verfügbar"، "Veröffentlicht am 20. Aug.
16:52"**. أي أن جوجل وافقت على المراجعة، والتطبيق **متاح الآن فعلياً لأي مستخدم
في 177 دولة/منطقة** — ليس اختباراً مغلقاً بعد الآن. هذا يُغلق نهائياً بند "أندرويد
وصل للإنتاج" (كان معلَّقاً بسبب مشكلة versionCode 10/11 الموثَّقة أدناه).

⚠️ **مهم**: صفحة الهبوط لا تزال تحتوي كلمة `prelaunch` عمداً — الخطة الأصلية كانت
حذفها بعد نشر **الاثنين معاً** (أندرويد وiOS). بما أن **iOS لا يزال عالقاً** (انظر
البند 6 أدناه، بانتظار Apple Support)، **لا تُحذف `prelaunch` بعد** حتى لا يصل
مستخدمو iOS الحقيقيون القادمون من نفس الصفحة لتجربة شراء غير مكتملة.

**تنظيف Firestore (22.08.2026)**: كان 8 مختبِرين قد حصلوا على وصول كامل لكل
الحروف عبر بطاقات Stripe تجريبية (Test Mode) قبل تفعيل Stripe Live. جرى تحديد
الفرق بدقة عبر بادئة حقل `stripeSessionId` في كل وثيقة `users/{uid}` — `cs_test_`
= تجريبي، `cs_live_` = حقيقي — وضُبط `subscribed: false` يدوياً للثمانية عبر
Firebase Console (Firestore → users). الوثيقة الوحيدة الحقيقية
(`ExUFgL2R9nRb8YrehHpMP91bkAg2`، أول اشتراك حقيقي للمستخدم نفسه بتاريخ
22.08.2026) لم تُلمَس. لا علاقة لهذا بأي إصدار تطبيق — تغيير أثّر فوراً عبر
`onSnapshot` بلا تحديث أو إعادة تنزيل.

## سابقاً (17.08.2026): versionCode 10 كان يحمل Billing Library القديمة فعلياً رغم توثيق سابق يقول العكس — أُصلح ورُفع versionCode 11 لمراجعة جوجل

**اكتُشفت المشكلة من واجهة Google Play Console نفسها**، ليس من الكود: صفحة "Neueste
Releases und Bundles" أظهرت أن إصدار الإنتاج **10 (1.8) كانت حالته "Entwurf"
(مسودّة) وليس "Wird überprüft" (قيد المراجعة) كما وثّق HANDOFF السابق** — لم
يُرسَل للمراجعة فعلياً قط، توقّف عند خطوة الحفظ فقط. وفتح تفاصيل الحزمة كشف
**"API-Level: 22 oder höher"**، أي أنها لا تحمل إصلاح Billing Library 9.1.0 /
minSdk 23 الموثَّق في قسم "Google Play Billing Library" أدناه.

**السبب الجذري (تأكّد من تواريخ commits بالضبط)**: حزمة versionCode 10 بُنيت
ورُفعت لجوجل الساعة 17:12 بتاريخ 13.08.2026 — لكن commit إصلاح Billing
Library/minSdk (`5e07b59`) حدث الساعة 20:44 من **نفس اليوم**، أي بعد رفع الحزمة
بأكثر من 3 ساعات. الحزمة المرفوعة إذاً بُنيت من كود **قبل** الإصلاح، رغم أن كود
`main` نفسه كان يحتوي الإصلاح لاحقاً — درس: **تاريخ commit الإصلاح لا يعني أن كل
حزمة مبنية لاحقاً تتضمّنه تلقائياً؛ يجب التحقق من الحزمة الفعلية المرفوعة، لا من
سجل Git وحده.**

**الإصلاح المُنفَّذ ويُتحقَّق منه خطوة بخطوة**:
1. رفع `versionCode` إلى **11** (`f22685c`)، `versionName` بقي "1.8" (لا تغيير
   وظيفي، فقط إعادة تعبئة صحيحة).
2. تحقّقتُ قبل البناء: الباتش (`patch-package`) مُطبَّق فعلياً في `node_modules`،
   و`minSdkVersion = 23` موجود في `variables.gradle`.
3. بُني عبر `./gradlew bundleRelease` — **BUILD SUCCESSFUL**، وتحقّقت من الملف
   الناتج (`app-release.aab`, ~128MB) فعلياً بعد البناء.
4. تحقّقتُ من الـmanifest الفعلي المُعالَج للحزمة
   (`bundle_manifest/release/processApplicationManifestReleaseForBundle/AndroidManifest.xml`،
   بنفس توقيت البناء) — يحتوي **`minSdkVersion="23"`** فعلياً، لا افتراضاً.
5. رُفعت الحزمة الجديدة في Play Console، **حُذفت حزمة 10 القديمة من نفس
   الإصدار** (لتفادي وجود حزمتين كاملتين متعارضتين)، وتأكّد جوجل كونزول نفسه أن
   11 (1.8) تحمل **"API-Level: 23 oder höher"**.
6. **الإرسال الفعلي**: اكتُشف أن "Speichern" في صفحة المعاينة **يحفظ فقط ولا
   يرسل للمراجعة** — الإرسال الحقيقي يتطلّب خطوة منفصلة من صفحة
   "Veröffentlichungen – Übersicht" بزر "X Änderung(en) zur Überprüfung
   einreichen". **هذه على الأرجح بالضبط النقطة التي توقّف عندها versionCode 10
   سابقاً ولم يُكتَشف.** نُفِّذت هذه الخطوة بنجاح لـversionCode 11 — الحالة
   الآن **"Änderungen, die überprüft werden"** (قيد المراجعة فعلياً، تأكّدنا من
   تغيّر عنوان القسم في الواجهة).

**تحذير غير حاسم (لم يُعالَج، ليس عائقاً)**: تحذير Play Console عن غياب ملف
كشف تعتيم (Deobfuscation/R8 mapping file) — اختياري، يخص فقط تحليل الأعطال
المستقبلية، لا يمنع النشر.

## سابقاً (14.08.2026): iOS كان يتعطّل 100%، أُصلح؛ أندرويد وصل فعلياً لـProduction (لكن انظر التصحيح أعلاه)؛ عالقون في ربط بناء iOS بأول مراجعة اشتراك

### 1) 🔴 اكتُشف وأُصلح: تطبيق iOS كان يتعطّل فوراً عند كل إقلاع (100%)

البناء 2 (كان على TestFlight) **رُفع لـCodemagic قبل** commit تسجيل Firebase على iOS
بأكثر من ساعة (تأكّدنا من توقيت git بالضبط) — أي أنه **لا يحتوي `GoogleService-Info.plist`
إطلاقاً**. عند الإقلاع، `FirebaseCrashlyticsPlugin` يستدعي `FIRApp.configure()` تلقائياً
(بلا أي تدخّل من كودنا)، وبما أن الإعداد مفقود يرمي Firebase استثناءً غير مُلتقَط →
SIGABRT فوري، **قبل** ظهور أي واجهة.

**كيف اكتُشف**: أخو المستخدم جرّب التطبيق فتعطّل. حمّلنا ملف `.ips` من App Store Connect
(TestFlight → Crashs → لقطة السحابة) وقرأنا الـ stack trace مباشرة — أظهر بوضوح
`FirebaseCore +[FIRApp configure]` في أعلى `Last Exception Backtrace`. هذا أثبت السبب
بيقين 100%، لا تخمين.

**الإصلاح**: الكود الحالي على `main` يحتوي فعلاً كل تسجيل Firebase الصحيح (مُنجَز من
جلسة سابقة) — فقط رفعنا رقم البناء لـ**3** (`CURRENT_PROJECT_VERSION`)، بنينا عبر
Codemagic، ثم لاحقاً **4** (يضمّ إصلاح "زر التالي" أدناه). **تحقّقنا حيّاً**: أخو
المستخدم حدّث لبناء 3 عبر TestFlight، فتح التطبيق، عمل بلا أي تعطّل.

**درس مهم للمستقبل**: أي مكوّن Firebase/Native SDK يُهيَّأ تلقائياً عند تحميل الإضافة
(Plugin load) — تحقّق دائماً أن ملفات الإعداد (`GoogleService-Info.plist`،
`google-services.json`) موجودة فعلاً في **نفس البناء** المرفوع، لا فقط في الفرع.

### 2) 🎉 أندرويد وصل فعلياً لـProduction (Google وافقت على Produktionszugriff)

بعد الموافقة، بنينا ورفعنا مباشرة (بلا المرور بالاختبار المغلق مجدداً):
- **versionCode 9 / 1.7**: رُفع أولاً للاختبار المغلق (قبل وصول الموافقة).
- **versionCode 10 / 1.8**: يضمّ كل شيء (9 + إصلاح زر التالي) — **رُفع مباشرة لمسار
  Production** واختير "Alle Länder" (كل الدول، لا استثناء فرنسا هنا — القيد كان خاصاً
  بآبل فقط). أُرسل للمراجعة بنجاح، **الحالة الآن: "Wird überprüft" (قيد مراجعة Google)**.
- ملاحظة تقنية: `Versionshinweise` (ملاحظات الإصدار) في Play Console تتطلّب صيغة خاصة
  بوسوم اللغة، وليس نصاً عادياً:
  ```
  <ar>
  نص الملاحظات هنا
  </ar>
  ```
  رمز اللغة `ar` فقط بلا لاحقة إقليمية (`ar-SA` مرفوض/"Nicht unterstützte Sprache").
- إقرار **Werbe-ID (Advertising ID)** كان يحتاج إجابة "Ja" (نعم) وليس "Nein" —
  Firebase Analytics يُدرج إذن `com.google.android.gms.permission.AD_ID` فعلياً في
  **manifest الحزمة (Bundle) النهائي**، حتى لو لم يظهر في `merged_manifest` الخاص
  بـAPK العادي (تحقّق من
  `app/build/intermediates/bundle_manifest/release/.../AndroidManifest.xml` تحديداً
  لا غيره). السبب المختار للاستخدام: "Analyse" فقط (لا إعلانات فعلية في الكود).

### 3) 🔴 تحديث حرج: Google Play Billing Library (مهلة 31.08.2026)

Google Play يرفض أي تحديث يستخدم Billing Library أقل من v8 اعتباراً من 31.08.2026.
مكتبة `@squareetlabs/capacitor-subscriptions` (تُستخدم فقط لتدفّق شراء iOS، لكنها تحمل
تبعية Android أيضاً حتى لو غير مُستدعاة من الواجهة) كانت على 7.1.1.

- رُفعت لـ**9.1.0** (أحدث إصدار، يمنح مهلة حتى 2028).
- هذا كسر واجهات برمجية (breaking API changes) داخل الإضافة نفسها — أُصلحت يدوياً:
  - `queryProductDetailsAsync` الآن يُرجع `QueryProductDetailsResult` وليس `List`
    مباشرة → `.getProductDetailsList().get(0)`.
  - `queryPurchaseHistoryAsync`/`QueryPurchaseHistoryParams`/`PurchaseHistoryRecord`
    **أُزيلت بالكامل** من المكتبة — أُعيدت كتابة `getLatestTransaction()` لاستخدام
    `queryPurchasesAsync` (نفس نمط `getCurrentEntitlements()` الموجود أصلاً).
- `minSdkVersion` رُفع من 22 إلى **23** (متطلَّب من المكتبة الجديدة، يستثني فقط أجهزة
  2015 وما قبل).
- ⚠️ **الإضافة موجودة داخل `node_modules`** (غير متتبَّعة في git أصلاً) — التعديل
  مُثبَّت بشكل دائم عبر **`patch-package`**: `android-app/patches/@squareetlabs+...patch`
  + سكربت `postinstall` في `android-app/package.json`. **أي `npm install` مستقبلي
  سيُطبِّق التصحيح تلقائياً — لا تحذف مجلد `patches/`**.
- **تحقّقتُ حيّاً على المحاكي**: هذا مهم لأن `BillingClient.startConnection()` يعمل
  تلقائياً عند كل إقلاع تطبيق على أندرويد (بغضّ النظر عن الاستخدام الفعلي)، فكان خطر
  تعطّل حقيقي شبيه ببند iOS أعلاه، وليس مجرد خطأ ترجمة (compile) فقط.

### 4) إصلاح واجهة: "زر التالي" لم يكن يظهر بلا سحب الشاشة يدوياً

طفل قد لا ينتبه لضرورة السحب لأعلى لرؤية زر "التالي ←" بعد إجابة صحيحة. أُضيفت دالة
`revealNextBtn()` (بديلة عن `document.querySelector('.btn-next').classList.add('show')`
المباشرة في 8 مواضع) تُمرّر الشاشة تلقائياً (`scrollIntoView smooth`) عند ظهور الزر.
لا تغيير تصميمي، فقط سلوك إضافي. **تحقّقتُ حيّاً على المحاكي** (حالة اصطناعية بفائض
محتوى) أن التمرير يحدث فعلاً عند الحاجة، ولا يحدث شيء إن كان الزر ظاهراً أصلاً.

### 5) Stripe: SEPA مفعَّل + ملف الشركة مكتمل

- **SEPA-Lastschriftverfahren مفعَّل** (بانتظار موافقة Stripe التلقائية) — يدعم
  الدفعات المتكررة، مناسب للسوق الألماني.
- **رقم الهاتف وُثِّق** (كان ناقصاً، يمنع سحب الأموال Auszahlungen).
- **Kontostatus تحقّقنا**: "Es sind keine aktiven Aufgaben abzuschließen" — لا نواقص
  أخرى (فقط Cartes Bancaires الفرنسي معلَّق، غير مؤثِّر).
- تصحيح نص السعر: عرض أندرويد كان يقول "2 يورو" بينما Stripe الفعلي **1.99€** — صُحِّح
  النص في `horofi-v11-9-29.html` (`isIOS ? '2.99...' : '1.99...'`).

### 6) 🔴 عالقون حالياً: لا يمكن ربط بناء iOS (4) بأول مراجعة اشتراك — بانتظار Apple Support

آبل تشترط أن أول اشتراك تلقائي التجديد **يُرسَل مع نسخة تطبيق (Build) في نفس الحزمة**.
لكن على صفحة "iOS-App Version 1.0"، قسم "Build" يعرض البناء القديم (2) من دورة الرفض
الأولى، **ولا يظهر أي زر "+" لتغييره** كما تصف توثيق آبل الرسمي بالضبط. جُرِّب:
- إزالة البناء من الصف (لا خيار).
- "Neue Übermittlung erstellen" (إنشاء إرسال جديد) — نفس المشكلة.
- تكبير الشاشة/تصغير التكبير (Zoom) تحسّباً لواجهة متجاوبة تُخفي الزر — لا فرق.
- كل الأماكن المحتملة لزر ⊕ (بجانب "Build"، بجانب "Element bereit zur Übermittlung"
  في نافذة المسودة) — لا يوجد.

هذا يطابق شكاوى حقيقية من مطوّرين آخرين على منتديات آبل لنفس المشكلة بالضبط. **أُرسلت
تذكرة دعم لآبل** (developer.apple.com/contact → App-Rezension → Andere Fragen) تصف
المشكلة بدقة وتطلب حلاً فورياً + تأكيداً أنها لن تتكرر. **بانتظار الردّ (لم يصل شيء
حتى 19.08.2026).**

**مسارات إضافية جُرِّبت في 19.08.2026 — لا تُكرَّر، نفس النتيجة الميتة بالضبط**:
آبل غيّرت الواجهة وأضافت ملاحظة زرقاء على صفحة "iOS-App Version 1.0" تقول إن
الاشتراكات الآن تُرسَل من قسم **"Abos"** مباشرة وليس من صفحة الإصدار (نص: "Füge
deiner Übermittlung eine App-Version hinzu"). تبعاً لذلك جُرِّب:
1. من **Abos → اشتراك صحّح حروفك → "Zur Prüfung hinzufügen" → "Übermittlungsentwurf
   (1)"** (المسودة القديمة من 22:05): نفس التحذير — "Kann nicht zur Prüfung
   übermittelt werden... füge eine App-Version hinzu"، بلا أي عنصر تحكّم لإضافتها.
2. من نفس الزر، **"Neue Übermittlung erstellen"** (مسودة جديدة تماماً، الساعة
   21:52): أضافت مجموعة الاشتراك (Abo-Gruppe) كعنصر بدل الاشتراك الفردي، لكن نفس
   الرسالة بالحرف ظهرت مجدداً — **لا يوجد أي عنصر تحكّم مرئي في أي من اللوحتين
   لإرفاق إصدار تطبيق**، بغضّ النظر عن نقطة الدخول.

**الخلاصة**: جُرِّبت الآن كل نقاط الدخول المعقولة (صفحة الإصدار، صفحة الاشتراك،
مسودة قديمة، مسودة جديدة) ووصلت كلها لنفس الجدار. المشكلة في واجهة آبل نفسها، لا في
طريقة تنقّلنا. **لا فائدة من إعادة تجربة أي من هذين المسارين لاحقاً** — الحل الوحيد
المتبقي هو انتظار ردّ Apple Support الفعلي.

3. **جُرِّب أيضاً (19.08.2026)**: إزالة عنصر الاشتراك من "Element bereit zur
   Übermittlung" في المسودة (بافتراض أن ذلك قد "يُحرِّر" جدول Build في صفحة
   الإصدار). **لم ينجح** — كما كان متوقَّعاً، جدول Build مُقفَل لسبب مستقل تماماً
   عن وجود مسودة اشتراك معلَّقة (كان مُقفَلاً من قبل ظهور أي مسودة اشتراك أصلاً).
   **لا تُكرَّر هذه المحاولة أيضاً.**

4. **جُرِّب أيضاً (22.08.2026)**: البحث عن طريقة لإنشاء **رقم إصدار iOS جديد**
   (مثل 1.1) بدل "إرسال جديد" داخل نفس الإصدار 1.0 العالق — بافتراض أن إصداراً
   جديداً كلياً لن يحمل نفس إشارة البناء 2 العالقة. **لا يوجد هذا الخيار إطلاقاً**
   حالياً: تحقّقنا من كامل الشريط الجانبي لصفحة "iOS-App"، الخيار الوحيد المتاح هو
   "Plattform hinzufügen" (إضافة منصّة جديدة كـmacOS، شيء مختلف تماماً)، لا يوجد
   "+ Version". السبب الأرجح: آبل لا تسمح بإصدار رقم جديد طالما لم يُعتمَد إصدار
   واحد على الأقل ويُنشر فعلياً من قبل. **لا تُكرَّر هذه المحاولة أيضاً** — لا
   فائدة من البحث عن هذا الخيار مجدداً قبل أن يُنشر أول إصدار فعلياً.

**الخلاصة النهائية بعد استنفاد كل المسارات المعقولة داخل الواجهة (22.08.2026)**:
لا حل تقني متبقٍّ نجرّبه بأنفسنا. الخطوة التالية الوحيدة المنطقية هي **تصعيد
المتابعة مع Apple** مباشرة (الرد على التذكرة الأصلية لسؤالهم عن حالتها، أو طلب
اتصال هاتفي من دعم المطوّرين عبر developer.apple.com/contact، أو البحث في
منتديات آبل الرسمية عن حل من مطوّر واجه نفس العقدة بالضبط) — وليس مزيداً من
تجربة مسارات الواجهة.

⚠️ **تحذير مهم (19.08.2026)**: لا تحذف أي **Bundle ID** أو **App Record** من
developer.apple.com كحل مقترح لهذه المشكلة — هذا إجراء مدمّر شبه نهائي (يقطع
الارتباط بكل تاريخ التطبيق: الشهادات، TestFlight، إلخ) **ولا علاقة له بمشكلتنا
الفعلية أصلاً**. مشكلتنا تخص اختيار/تبديل **رقم البناء (Build)** داخل مراجعة
موجودة، وليست مشكلة حذف معرّف حزمة (Bundle ID) — نصيحة مشابهة ظهرت من أداة ذكاء
اصطناعي أخرى بسبب التباس بين المصطلحين، رُفضت.

✅ **أُنجز أثناء ذلك** (لا علاقة له بالعقدة أعلاه):
- صورة مراجعة الاشتراك (Screenshot في Prüfinformationen): اكتُشف أن آبل تطلب **مقاساً
  دقيقاً بالضبط 640×920 بكسل** (وليس "على الأقل" كما ظُنّ سابقاً) — أي بكسل واحد زيادة
  يُرفض برسالة "Mindestens ein Screenshot weist falsche Maße auf". وُلِّدت صورة صحيحة
  بهذا المقاس بالضبط (`chrome --headless=new --window-size=640,920 --screenshot=...`
  بلا `--force-device-scale-factor` الذي يُفسِد التوسيط)، رُفعت بنجاح.
- صورة "Bild (optional)" الترويجية (1024×1024): استُخدم
  `android-app/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`
  (الشعار الرسمي بدقة كاملة) بدل `img/icon.png` القديم (192×192 فقط، غير صالح).
- Lokalisierung لمجموعة الاشتراك: أُضيفت العربية باسم عرض "اشتراك صحّح حروفك".

### 7) أمان: ملفان حسّاسان كانا في مجلد المشروع

`رقم سر الشهادة.jpg` (كلمة مرور شهادة توقيع Codemagic بنص صريح) و
`Apple Developer Portal interegration.jpg` (Issuer ID/Key ID الحقيقيَّين لمفتاح
App Store Connect API) كانا ملفَّين فضفاضين في جذر المشروع (غير مُلتزَمَين بـgit، لكن
خطر إن شُورك المجلد). **أُخرجا من المجلد بمعرفة المستخدم** — تحقّقتُ من زوالهما.

### مهام متبقية فعلية

- **جاهز وينتظر الدمج في بناء قادم (22.08.2026)**: ميزة In-App Review الرسمية
  (`@capacitor-community/in-app-review`, commit `d399360`) — Android
  ReviewManager وiOS SKStoreReviewController، تُستدعى بلا تصفية حسب النجوم
  (تفادياً لمخالفة إرشادات آبل/جوجل)، تحقّقت منها حيّاً على المحاكي (تعمل بلا
  تعطّل). **لم تُرفع لأي متجر بعد** — أول versionCode أندرويد قادم وأول بناء iOS
  قادم يجب أن يتضمّناها. لا تنسَ رفع `versionCode` (سيكون 12) قبل البناء التالي.
- **بانتظار ردّ Apple Support** بخصوص عقدة ربط البناء (البند 6) — الأولوية القصوى.
- بعد حلّها: ربط البناء 4 بمراجعة الاشتراك، إرسال الاثنين معاً لمراجعة آبل الكاملة.
- ~~بانتظار ردّ Google على مراجعة Production لأندرويد~~ — ✅ **مُنجَز 20.08.2026**:
  versionCode 11 (1.8) منشور فعلياً وعلناً على Google Play في 177 دولة (تحقّقنا
  من صفحة Produktion → Releases مباشرة: "Bei Google Play verfügbar").
- بعد نشر **الاثنين** فعلياً للجمهور (أندرويد ✅، iOS لا يزال معلَّقاً): حذف
  `prelaunch` من `index.html` (لا يزال موجوداً عمداً حتى ينشر iOS أيضاً).
- وثيقة فرنسا لآبل (ANSSI Export Compliance) — مؤجَّلة بقرار المستخدم، iOS مستبعد
  فرنسا مؤقتاً من التوزيع.
- صور `img/*-Kopie.png` المكرَّرة — تنظيف اختياري غير عاجل.
- توسعة قائمة بلدان اختيار الطفل لكل دول العالم (نسبة عربية مُشكَّلة لكل دولة) —
  مؤجَّلة بطلب المستخدم صراحة.

## سابقاً: اشتراك iOS الحقيقي + الإعداد المالي/الضريبي (08.08.2026)

- **الدفع الحقيقي على iOS مُنجَز بالكامل** (كود + خادم): `com.Horofi.monthly2eur`،
  2.99€/شهر، مكوّن `@squareetlabs/capacitor-subscriptions@1.0.25` (Capacitor 6،
  **ليس** RevenueCat)، مكتبة `@apple/app-store-server-library` على الخادم للتحقق
  الحقيقي (`verifyAppleSubscription`, `appleServerNotifications`).
- **App Store Connect المالي/الضريبي/التعاقدي مكتمل بالكامل**: Paid Apps Agreement
  موقَّع، حساب بنكي Aktiv، نماذج ضريبية W-8BEN Aktiv، طلب Apple Small Business Program
  مُرسَل، DAC7 "Nein" (ليس تطبيق وساطة). Sandbox Tester:
  `arch.mohamedmerie+sandbox1@gmail.com` / Deutschland.
- **رفض Apple الأول (Submission 163ac6d2) أُصلح بالكامل** — 3 أسباب: اختيار جنس الطفل
  إلزامي بلا حظر، إخفاء زر شراء Stripe على iOS (`Capacitor.getPlatform()==='ios'`)،
  الإجابة الصادقة عن أسئلة التحليلات/الإعلانات. `IPHONEOS_DEPLOYMENT_TARGET` رُفع لـ15.0.
- **مزامنة بيانات الأطفال لـFirestore**: كانت في `localStorage` فقط (خطر ضياع حقيقي)،
  الآن `syncChildrenToCloud()`/`pullChildrenFromCloud()` + حماية `firestore.rules`
  الكاملة لحقول الاشتراك (لا تُكتَب إلا من Cloud Function).
- **Firebase على iOS أُنشئ من الصفر** (`firebase apps:create IOS`)، معالج أخطاء JS
  عام (`_reportJsError`) يصل لـCrashlytics.

## بنية المشروع

- **الملف الرئيسي**: `horofi-v11-9-29.html` — مصدر الحقيقة الوحيد لأندرويد وiOS معاً
  (فحص المنصّة عبر `Capacitor.getPlatform()` داخل نفس الملف). كل تعديل يُنسخ بعده:
  ```
  cp horofi-v11-9-29.html android-app/www/index.html
  cd android-app && npx cap sync android && npx cap sync ios
  ```
- **أندرويد**: `android-app/android/` — بناء محلي:
  ```
  cd android-app/android && JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew bundleRelease
  ```
  رفع رقم `versionCode`/`versionName` في `app/build.gradle` قبل كل بناء release جديد.
  اختبار عبر محاكي `Pixel_8_stable` **فقط** (لا تُشغِّل محاكيَين معاً، تحقّق بـ
  `tasklist | grep qemu` أولاً). عند تعليق adb بعد استخدام طويل: `adb kill-server`
  ثم أعد تشغيل المحاكي نفسه (ليس فقط adb) إن ظهر الجهاز "offline".
- **iOS**: `android-app/ios/` — **لا بناء محلي على ويندوز**، فقط عبر Codemagic
  (رمز API يُطلب من المستخدم في كل جلسة، لا يُحفظ):
  ```bash
  TOKEN='...'; APP_ID='6a673beaf1d3a42a0686a30b'
  curl -s -X POST -H "x-auth-token: $TOKEN" -H "Content-Type: application/json" \
    -d "{\"appId\":\"$APP_ID\",\"workflowId\":\"ios-testflight\",\"branch\":\"main\"}" \
    https://api.codemagic.io/builds
  ```
  رفع `CURRENT_PROJECT_VERSION` في `project.pbxproj` قبل كل بناء (آبل ترفض تكرار الرقم).
  `ios-simulator-build` للتحقق السريع بلا نشر، `ios-testflight` (موقَّع، يرفع فعلياً)
  فقط بتأكيد صريح من المستخدم في كل مرة.
- **الخلفية**: `horofi/index.js` — أربع Cloud Functions (`stripeWebhook`،
  `createPortalSession`، `verifyAppleSubscription`، `appleServerNotifications`)، تُنشر
  معاً عبر `firebase deploy --only functions:horofi`. `firestore.rules` منفصلة.

## أدوات التصحيح المهمة

**Chrome DevTools Protocol على WebView التطبيق** (أدقّ من `adb shell input tap`):
```bash
ADB="/c/Users/m-hm1/AppData/Local/Android/Sdk/platform-tools/adb.exe"
"$ADB" shell cat /proc/net/unix | grep -o "webview_devtools_remote[^ ]*"
"$ADB" forward tcp:9333 localabstract:webview_devtools_remote_XXXXX
curl -s http://localhost:9333/json   # يعطي webSocketDebuggerUrl
```
ثم `node cdp_eval.js "<wsUrl>" "<jsExpression>"` (سكربت Node صغير بـ`Runtime.evaluate`،
بلا حزم خارجية — أعد كتابته من هذا الملف إن لزم، انظر تاريخ git لنسخة كاملة).

**لقطات شاشة بمقاس دقيق لمتاجر التطبيقات**: `chrome.exe --headless=new
--window-size=W,H --screenshot=out.png "file:///path.html"` — **بلا**
`--force-device-scale-factor` (يُفسِد التوسيط RTL). لمحتوى JS ديناميكي، أضف `<script>`
في نهاية `<body>` يزيّف `window.Capacitor` ويستدعي دالة التنقّل المطلوبة مباشرة.

## معروف/مؤجَّل

- قائمة كاملة لما قبل الإطلاق موجودة في ذاكرة كلود الدائمة
  (`horofi_prelaunch_checklist.md`).
- دراسة استقرار إنتاج شاملة نُشرت كـArtifact في جلسة سابقة.
