# ملف تسليم — صحّح حروفك (29 يوليو 2026)

## ⚠️ الأحدث: إعداد iOS الموقَّع + بدء تسليم App Store + Git/Firebase (29.07.2026)

### Git وFirebase (منجَز بالكامل)
- المشروع الآن يُحفَظ فعلياً على GitHub (`archmohamedmerie-coder/horofi`, فرع `main`) —
  عادة العمل صارت: بعد كل تعديل مهم، commit ثم push (بعد موافقة المستخدم).
  **لا تترك تعديلات غير محفوظة لفترة طويلة.**
- `horofi-release.keystore` و`keystore.properties` منسوخان احتياطياً على Google Drive
  للمستخدم (مجلد خاص غير مُشارَك) — إن فُقد الجهاز الحالي، النسخة الاحتياطية موجودة.
- **Firebase Crashlytics + Analytics** مفعَّلان في مشروع أندرويد (`google-services.json`
  مُضاف، تبعيات Gradle مُضافة، تحقّقتُ حيّاً أن التطبيق يعمل بلا أعطال). لا توجد
  أحداث تحليلات مخصّصة بعد (فقط الافتراضية التلقائية) — مهمة منفصلة لاحقاً.
- إصلاحات UX أخيرة على تمارين الكتابة: الشخصية الكرتونية أُعيد تموضعها 3 مرات
  حسب طلبات دقيقة (حرف مفرد: أسفل اللوح يمين، كلمة: داخل اللوح يمين، تجميع: داخل
  لوح الكلمة يسار) — كل التفاصيل بالحجم/الموضع بالبكسل موجودة في CSS الحالي، لا
  حاجة لإعادة القراءة إلا لو طُلب تغييرها مجدداً.

### iOS: بناء موقَّع ناجح أخيراً عبر Codemagic (بعد جلسة تصحيح طويلة جداً)
بعد عشرات المحاولات الفاشلة (راجع commits `9942..23067ce` في `codemagic.yaml` و
`project.pbxproj` لتفاصيل كل محاولة)، **نجح البناء الموقَّع ووصل فعلياً لـApp
Store Connect**. الدروس الحاسمة التي استغرقت وقتاً طويلاً لاكتشافها:

1. **`keychain initialize` كان يمسح الشهادة المستوردة تلقائياً** — عندما تكون
   `environment.ios_signing.certificates/provisioning_profiles` معرَّفة في
   `codemagic.yaml`، Codemagic يستورد الشهادة تلقائياً لسلسلة مفاتيح خاصة به قبل
   أي سكربت. استدعاء `keychain initialize` يدوياً بعد ذلك **يُنشئ سلسلة فارغة
   ويستبدلها بها** — فتضيع الشهادة. **الحل: لا تستخدم `keychain initialize` إطلاقاً
   عندما تعتمد على `ios_signing.certificates` المرجعية.** هذا كان السبب الجذري
   الحقيقي وراء خطأ "No … signing certificate … with a private key was found"
   المتكرر — لم يكن أبداً خطأ في الشهادة نفسها (جُرِّبت 3 شهادات مختلفة بلا فائدة
   قبل اكتشاف هذا).
2. **لا تخلط بين وضعَي التوقيع**: `ios_signing.distribution_type + bundle_identifier`
   (تلقائي بالكامل) و`ios_signing.certificates + provisioning_profiles` (يدوي
   بالشهادات المخزَّنة) **متعارضان** — Codemagic يرفض ملف YAML إن وُجدا معاً.
3. **إنشاء شهادة عبر CLI (`app-store-connect fetch-signing-files --create`)
   غير موثوق** — فشل مراراً بخطأ "Cannot save Signing Certificates without
   certificate private key" بصرف النظر عن حالة الحساب. **البديل الموثوق**: زر
   **"Generate certificate"** داخل واجهة Codemagic نفسها (Settings → Code signing
   identities → iOS certificates) — يُنشئ Codemagic المفتاح الخاص ويحتفظ به
   داخلياً من البداية للنهاية، مضمون التطابق دائماً.
4. عند تكرار محاولات إنشاء الشهادات، **تُصبح صفحة "Certificates" في Apple
   Developer Portal مليئة بشهادات متشابهة الاسم** — عمود **"CREATED BY"** هو
   الطريقة الوحيدة الموثوقة للتمييز بينها (يدوياً = من شخص، تلقائياً = من API Key).
   ⚠️ **لا تُلغِ (Revoke) شهادة من هناك إلا بعد التأكّد التام من العمود** — حدث
   خطأ سابق بإلغاء الشهادة الصحيحة بالخطأ مع الخطأ.
5. **الإعداد الحالي النهائي العامل**: `codemagic.yaml` (workflow `ios-testflight`)
   يستخدم `ios_signing.certificates: [Horofi_Dist_Cert_v3]` و
   `provisioning_profiles: [Horofi_AppStore_Profile]` (كلاهما من إنشاء Codemagic
   نفسه)، بلا `keychain initialize`، مع `xcode-project use-profiles` ثم
   `xcode-project build-ipa`. `project.pbxproj` للهدف Release مضبوط يدوياً على
   `CODE_SIGN_STYLE = Manual` (لم يعد هذا التعديل هو الحل الفعلي كما تبيّن لاحقاً،
   لكن إبقاؤه غير ضار).
6. **مفتاح API لـApp Store Connect**: اسمه في Codemagic "Codemagic-Admin"، صلاحية
   **Admin** (ليس App Manager — الأدنى قد يمنع إنشاء الشهادات). Team ID: `D4DJFFY3GX`.

### تسليم App Store Connect — الحالة الحالية (غير مكتمل)
البناء الموقَّع (رقم 1، نسخة 1.0) مرفوع بنجاح لـApp Store Connect. المُنجَز من
نموذج التسليم:
- Werbetexte / Beschreibung / Schlüsselwörter: مُعبّأة (نص جاهز في محادثة الجلسة
  إن احتجت إعادة استخدامه لنسخة مستقبلية).
- Support-URL / Marketing-URL: `https://archmohamedmerie-coder.github.io/horofi/`
- Copyright: `© 2026 صحّح حروفك` (مطابق لتذييل صفحة الهبوط، وليس اقتراحي الأول).
- Datenschutz-URL: `https://archmohamedmerie-coder.github.io/horofi/privacy.html`
- Kategorie: Bildung. Preis: Gratis. Export Compliance: تشفير قياسي فقط،
  فرنسا = Nein (لتفادي متطلّبات توثيق إضافية). Veröffentlichung: يدوي (Manuell).
- Altersfreigaben: كل الأسئلة (Kindersicherung، Internetzugriff، إلخ) = NEIN.
- Anmeldeinformationen: تسجيل دخول مطلوب فعلياً (المستخدم أدخل بيانات تجريبية).
- **In-App-Käufe**: لا يوجد Stripe داخل نسخة iOS إطلاقاً — تم تجاهل هذا القسم بأمان.

⚠️ **غير مكتمل بعد**:
- بعض حقول Altersfreigaben (الخطوات 2-7) لم تُكمَل بعد.
- صور الشاشات (Screenshots): وُلِّدت 6 صور (3 iPhone 6.5" @1242×2688 + 3 iPad 13"
  @2064×2752) من لقطات محاكي قديمة محفوظة في scratchpad (سكربت Pillow بسيط:
  `resize.py`/`resize_ipad.py` في مجلد scratchpad الجلسة يُعيد قصّها/توسيطها بخلفية
  #FFF9F0). **المستخدم طلب 3 لقطات إضافية** (تمرين الاختيار من متعدد، صفحة
  مستوى مبتدئ/متوسط/متقدم، الصفحة الرئيسية بالشعار وشبكة الكلمات) **بكلا
  البُعدين** — لم تُنجَز بعد لأن **المحاكي عَلِق في حالة "offline" ولم يستجب**
  حتى بعد `adb kill-server`/`emu kill`. **نقطة الاستئناف**: أنهِ عملية المحاكي
  القديمة تماماً (قد تحتاج Task Manager يدوياً إن رفض adb)، شغّل AVD "Pixel_8"
  من جديد، ثبّت `app-debug.apk` (موجود بالفعل مبنياً في
  `android-app/android/app/build/outputs/apk/debug/`)، انتقل عبر CDP لهذه
  الشاشات الثلاث، التقط لقطات، ثم شغّل نفس سكربتَي resize.py.
- App-Informationen: Rechte-Informationen (حقوق المحتوى) وRechte-Fragen الأخرى
  لم تُؤكَّد بعد.
- Kontaktinformationen: رقم الهاتف كان فارغاً آخر مرة.
- زر "Zur Prüfung hinzufügen" (الإرسال النهائي للمراجعة) **لم يُضغَط بعد عمداً** —
  قرار المستخدم النهائي فقط.

## ⚠️ (سابقاً) الأشرطة العلوية اللاصقة كانت تنزلق خلف شريط حالة أندرويد عند التمرير (28.07.2026)

المستخدم لاحظ في لقطة حيّة (صفحة "تقرير الأهل") أن الشريط البنفسجي العلوي يبدأ
من الحافة العليا تماماً بلا أي فجوة لشريط حالة الهاتف (الساعة/الواي فاي/البطارية).

**السبب الجذري** (تحقّقت منه حيّاً عبر CDP بمقارنة موضع العنصر قبل/بعد التمرير،
لا تخميناً): `.topbar` و`.gtop` كانا يستخدمان `position:sticky; top:0`. عند
موضع التمرير صفر هذا يبدو صحيحاً لأن `body{padding-top:env(safe-area-inset-top)}`
يدفع المحتوى لأسفل أول مرة — لكن بمجرد التمرير للأسفل، العنصر اللاصق يلتصق بـ
`top:0` الحقيقي لإطار العرض (viewport) **لا** بحافة المنطقة الآمنة، وبما أن
تطبيق Capacitor هذا يُصيّر الـWebView خلف شريط الحالة (edge-to-edge)، ينزلق
الشريط اللاصق تحت شريط الحالة الحقيقي ويُغطّي جزءاً منه. أثبتّ هذا رقمياً:
`getBoundingClientRect().top` كان `0` بعد التمرير بدل البقاء عند ~51-61px.

**الإصلاح**: `top:0` → `top:env(safe-area-inset-top)` لكل من `.topbar` و`.gtop`،
وبالمثل `.global-mute-btn` (`position:fixed;top:12px` → `top:calc(env(safe-area-inset-top) + 12px)`،
كان يعاني نفس العلّة لكنها أشدّ — يظهر خلف شريط الحالة **دائماً** لا بعد التمرير
فقط، لأنه `fixed` غير مرتبط بموضع التمرير إطلاقاً).

تحقّقتُ حيّاً بعد الإصلاح: `getBoundingClientRect().top` يبقى عند نفس القيمة
(51/61px) قبل التمرير وبعده لكلا الشريطين، ولقطات الشاشة تؤكّد بقاء شريط حالة
أندرويد ظاهراً كاملاً فوق الشريط اللاصق دائماً.

## تكبير شبكة تتبّع شكل الحرف (28.07.2026)

`.trace-ref-grid span` (شبكة 3×3 لتتبّع شكل الحرف): من `3.2rem` إلى `6rem` +
`gap:2px 4px` على الشبكة نفسها لمنع تلامس الأعمدة عند الأشكال الممدودة
بالكشيدة على الجانبين (مثل "ـتـ" الوسطى). تحقّقتُ حيّاً بأوسع شكل ممكن (تاء
وسطى بعد حرف متصل) — لا تداخل بين الأعمدة رغم الحجم الكبير.

## علّتان صوتيتان حقيقيتان اكتُشفتا وأُصلحتا (28.07.2026)

**١) صوت "اكتب" يُنطَق بدل "تتبّع" في جولة التجميع** — المستخدم لاحظ في لقطة
حيّة أن جولة التجميع (بالنقر) تعرض نصّاً مكتوباً "اُكْتُبْ كَلِمَةَ..." (صحيح، لم
يتغيّر) لكن الصوت المنطوق يقول "تَتَبَّعْ..." (خطأ). السبب: `playWriteQuestionAudio`
كانت تشارك نفس ملف `word_prompt.mp3` بين جولتَي التتبّع اليدوي والتجميع، وحين
غُيِّر نص هذا الملف اليوم إلى "تتبّع" (لتطابق جولة التتبّع) انكسرت مطابقته لجولة
التجميع التي لم يتغيّر نصّها. **الإصلاح**: فرع مستقل لـ`kind==='assemble'` يُشغّل
ملفاً جديداً `audio/write/assemble_prompt.mp3` (بالنص القديم "اُكْتُبْ هَذِهِ
الْكَلِمَةَ مَوْصُولَة")، منفصل تماماً عن `word_prompt.mp3` (نص "تتبّع" الجديد).
وُلِّد الملف فعلاً (بمفتاح المستخدم، غير محفوظ) وتحقّقتُ حيّاً عبر CDP أن جولة
التجميع تُحمِّل `assemble_prompt.mp3` تحديداً (لا `word_prompt.mp3`) الآن.

**٢) صوت شارتَي الكتابة ("خطّاط الشكل"/"كاتب الكلمات") ينقطع فوراً بلا صوت** —
تحقّق تجريبي دقيق عبر CDP (وليس تخميناً) كشف السبب الجذري: `playStaticMP3` /
`playWordAudio` / `playSentenceAudio` كانت جميعها تستدعي `_stopCurrentAudio()`
(لإيقاف أي صوت سابق) ثم تُنشئ وتُشغّل عنصر `<audio>` **جديداً في نفس اللحظة
تماماً**. إن كان الصوت المُوقَف لم يستقرّ بعد (لا يزال في بداية تحميله/تشغيله)،
هذا التتابع الفوري يُسبّب في WebView أندرويد توقّفاً صامتاً شبه فوري (أقل من نصف
ثانية) للصوت الجديد — رغم أن `play()` ينجح ظاهرياً وحدث `playing` يُطلَق فعلاً،
فلا خطأ واضح يظهر في أي مكان. أثبتّ هذا بمقارنة تحكّمية مباشرة عبر CDP (تشغيل
مباشر = يكتمل بالكامل بلا مشاكل؛ إيقاف صوت طازج ثم تشغيل جديد فوراً = ينقطع خلال
<400ms؛ نفس التتابع مع فجوة 80ms = يكتمل بالكامل). **الإصلاح**: الدوال الثلاث
تتحقق الآن هل كان هناك صوت نشط فعلاً قبل الإيقاف؛ إن كان كذلك تُضيف مهلة 80ms
قبل بدء الصوت الجديد (لا تؤثر على الحالة الشائعة: بدء صوت نظيف بلا صوت سابق
يبقى فورياً كما كان). تحقّقتُ حيّاً: صوت شارة "خطّاط الشكل" يكتمل الآن بكامل
مدّته (~3.7 ثانية) بدل الانقطاع خلال أقل من نصف ثانية.

هذا ملخص حالة المشروع لبدء محادثة جديدة بفهم كامل دون إعادة شرح. احذف هذا الملف
أو حدّثه متى أصبح قديماً.

## الأحدث: 4 تحسينات على تدريب التتبّع الجديد (28.07.2026، بعد النشر مباشرة)

بعد أن جرّب المستخدم النسخة الأولى من تدريب التتبّع (القسم التالي)، طلب 4 تعديلات
نُفِّذت وتحقّقت جميعها حيّاً عبر CDP:

1. **حذف ML Kit نهائياً** — بما أنه صار كوداً ميتاً بعد التحويل، حُذف
   `HandwritingPlugin.java` كاملاً، وتبعية `com.google.mlkit:digital-ink-recognition`
   من `build.gradle`، وتسجيلها في `MainActivity.java` (رجعت لأبسط صيغة Capacitor
   افتراضية)، وكذلك كود تنزيل النموذج المبكّر (`window._hwReady`) من نهاية
   الملف. تحقّقتُ حيّاً: `window.Capacitor.Plugins.Handwriting` غير موجود الآن،
   والبناء نجح، والتطبيق يعمل طبيعياً.
2. **أذرع اتصال صريحة في بطاقات تمرين التجميع** — بطاقات `asm-tile` كانت تعرض
   الغليف الجاهز من الخط فقط (ذيل الاتصال فيه خفيف جداً)، فأضفتُ نفس تقنية
   "شجرة أشكال الحرف" (`.leaf-glyph.arm-*`): عنصر `ـ` إضافي بموضع مطلق `position:absolute`
   بنفس منسوب الحرف تماماً (لا أعلى ولا أسفل)، بكلاس جديد مستقل `.asm-glyph`
   (لأن `.leaf-glyph` الأصلي يعتمد `position:relative` من سياق `.tree-leaf` تحديداً
   ولا يعمل صحيحاً خارجه). الخريطة: `ini→يسار`, `med→الاثنان`, `fin→يمين`,
   `iso→بلا ذراع` (`ASM_ARM_CLASS`). تحقّقتُ حيّاً: الأذرع تظهر بوضوح ومحاذاة
   صحيحة لكل الأشكال.
3. **صوت جديد لجُمَل "تتبّع"** — نص الأسئلة تغيّر من "اُكْتُبْ..." إلى "تَتَبَّعْ..."
   نصّياً في الجولة السابقة لكن الصوت المسجَّل بقي بالصياغة القديمة. حدّثت
   `horofi_tts_write.py` (جملتا `build_phrases()`) وأعدت توليد كل الـ123 ملفاً
   (`audio/write/{char}_{pos}.mp3` × 122 + `word_prompt.mp3`) عبر ElevenLabs
   بمفتاح المستخدم (لم يُحفظ في أي ملف، استُخدم مباشرة كوسيط سطر أوامر مرة
   واحدة ثم لم يُعَد استخدامه). نُسخت الملفات الجديدة إلى `android-app/www/audio/write/`.
4. **شبكة تتبّع الكلمة: 5 صفوف → 4 صفوف بخط أكبر** — `Array(5)` صارت `Array(4)`
   في `buildTraceRound`، وحجم خط `.trace-word-grid span` كُبِّر من `1.9rem` إلى
   `2.6rem` لتسهيل التتبّع (كانت الكلمات الطويلة كـ"صليب" تبدو مزدحمة بخط صغير).

## متابعة فورية: تكبير خط الكلمة ضعفين + إصلاحان صغيران (28.07.2026)

بعد لقطة شاشة للنتيجة أعلاه، المستخدم طلب 3 أشياء إضافية، نُفِّذت وتحقّقت حيّاً:

1. **خط شبكة الكلمة ×2** — من `2.6rem` إلى `5.2rem` (طلب صريح "تكبير ضعفين").
   تحقّقتُ أن كلمة 5 أحرف بحروف صاعدة/نازلة ("قميص") لا تزال تُطبَّع بالكامل
   داخل اللوح بلا فيض أفقي أو رأسي عبر 4 الصفوف.
2. **صوتان عامّان ناقصان لشارتَي الكتابة** — `showBadgePopup` له مسار احتياطي
   أخير `audio/badges/${badge.id}.mp3` (بلا حرف/جنس) كان موجوداً في الكود منذ
   بداية الجلسة لكن الملفان لم يُوجَدا قط (`glyph_master.mp3`, `word_master.mp3`
   — راجع القسم القديم "3 إصلاحات..." أدناه حيث اكتُشفت هذه الثغرة أول مرة).
   أضفتُهما لـ`horofi_tts_writing_badges.py` (محتوى محايد الجنس لأن المسار بلا
   لاحقة: "شَارَةُ خَطَّاطِ الشَّكْلِ!" / "شَارَةُ كَاتِبِ الْكَلِمَاتِ!") وولّدتهما
   بمفتاح المستخدم (نفس سياسة عدم الحفظ). هذا المسار الاحتياطي نادراً ما يُستدعى
   فعلياً (المسار المخصّص لكل حرف/جنس يُمرَّر دائماً حالياً) لكنه صار آمناً الآن.
3. **إزالة ادّعاء "بإتقان" من نتيجة المسار المباشر** — رسالة `showDone()` في
   حالة `WRITING_ONLY && isPerfect` كانت تقول "تَدَرَّبْتَ عَلَى كِتَابَةِ حَرْف X
   **بِإِتْقَان**" رغم أن جولتَي الشكل/الكلمة صارتا تدريب تتبّع مضمون النجاح
   دائماً (لا تحقّق دقة إطلاقاً) — فـ"إتقان" وصفٌ غير صحيح الآن، وisPerfect عملياً
   يعكس دقة التجميع فقط. حُذفت الكلمة من [horofi-v11-9-29.html:4948](horofi-v11-9-29.html:4948)
   (لا صوت مرتبط بهذا السطر أصلاً في هذا المسار، فالإصلاح نصّي بحت بلا حاجة
   لصوت جديد). تحقّقتُ حيّاً عبر إجبار نتيجة كاملة على المحاكي.

## ⚠️ تحويل الكتابة اليدوية من اختبار إلى تدريب تتبّع (28.07.2026)

بعد جلسة كاملة من محاولات إصلاح الفحص الهندسي/ML Kit لصحة شكل الحرف/الكلمة
المكتوبين يدوياً (راجع القسم التالي للتاريخ الكامل — أُبقي عليه كسجل قرار)،
ثبت بشكل قاطع أن كليهما غير موثوق بما يكفي لتطبيق أطفال: الفحص الهندسي يرفض
حروفاً صحيحة كثيرة أو يقبل خربشات، وML Kit يضع الحرف الصحيح كأول احتمال في
41.4% فقط من الكتابة الحقيقية (حروف مفردة معزولة عن سياق كلمة — ليس ما دُرِّب
عليه النموذج). المستخدم قرّر تغيير الفكرة جذرياً بدل الاستمرار بالترقيع.

**التصميم الجديد لجولتَي `kind:'glyph'` و`kind:'word'`** (جولة `kind:'assemble'`
لم تتغيّر إطلاقاً — تبقى اختباراً حقيقياً بمقارنة رموز يونيكود قاطعة):
- الشكل/الكلمة الصحيحة تُعرَض دائماً على اللوح بلون رمادي شفاف
  (`rgba(90,60,140,.28)`, خط Amiri) — مكرَّرة 9 مرات (شبكة 3×3، `.trace-ref-grid`)
  للحرف المفرد، أو 4 مرات (صفوف تحت بعض بخط كبير 2.6rem، `.trace-word-grid`)
  للكلمة (كانت 5 صفوف بخط أصغر، قُلِّلت بطلب المستخدم لتسهيل التتبّع).
- الطفل يتتبّعها بإصبعه فوق قماشة رسم شفافة (`<canvas>` بنفس منطق التقاط
  `pointerdown/move/up` القديم، بلا تغيير).
- زر "تحقّق" يقبل **أي** محاولة رسم حقيقية (`_hasTraced`: ≥6 نقاط ومساحة
  امتداد ≥18px) دون أي حكم على الشكل — منحة نجاح مضمونة، لا محاولتان ولا خصم
  قلوب ولا كشف إجابة لهاتين الجولتين (بقي ذلك فقط لجولة التجميع).
- اللوح كُبِّر (`height:290px` → `440px`)، وزر "👁 ذكّرني بالشكل" حُذف نهائياً
  (لم يعد له معنى — الشكل ظاهر دائماً للتتبّع). زر "🔊 اسمع الكلمة" بقي لجولات
  الكلمة فقط.

**حُذفت نهائياً** كل كتلة الفحص الهندسي/ML Kit القديمة (~260 سطراً):
`checkWriting`, `checkGlyphNative`, `checkWordNative`, `_maskFromCanvas`,
`_renderTargetCanvas`, `_WRITE_TPL_FONTS`, `_dotSignature`, `_drawStrokesCanvas`,
`_components`, `_overlap`, `_inkBBoxFraction`, `_normArForMatch`, `wShowHint`,
`_writeOverlayFontSize`، و`evaluateWriteAnswer` استُبدلت بـ`evaluateTraceAnswer`
+ `_hasTraced`. دالة البناء `buildWriteRound` صارت تفوّض لدالة جديدة
`buildTraceRound` لجولتَي glyph/word (و`buildAssembleRound` كما هي لـassemble).

**التحقّق**: تم بناء الـAPK وتثبيته على المحاكي والتحقّق حيّاً عبر CDP + لقطات
شاشة: شبكة الحرف 3×3 تظهر صحيحة، ضربة واحدة حقيقية على اللوح قُبلت فوراً
(نقاط +15، تهنئة، شارات، بلا أي رفض)، وجولة التجميع تعمل تماماً كما كانت
(لم تُمسّ حينها). لم يُختبَر بعد على جهاز حقيقي (بدل
المحاكي) ولا نُشرت نسخة Play Console جديدة لهذه الميزة بعد.

## آخر جلسة سابقة: 3 إصلاحات في مرحلة اختبار الكتابة (تم التحقّق منها حيّاً عبر CDP)

المستخدم أرسل صوراً من مرحلة اختبار الكتابة تُظهر 3 مشاكل. جميعها شُخِّصت
بالسبب الجذري عبر CDP (وليس تخميناً) وأُصلحت وأُعيد بناء الـAPK وتثبيته
والتحقّق من كل إصلاح حيّاً على المحاكي:

1. **تداخل صوتَين عند الضغط على "التالي"**: `playStaticMP3`/`playSentenceAudio`/
   `playWordAudio` كانت تستبدل `window._currentAudio` بعنصر Audio جديد دون
   إيقاف السابق (خصوصاً حين تنتهي مهلة الأمان 6 ثوانٍ في `playStaticMP3` قبل
   أن ينتهي الصوت فعلياً). أضفتُ `_stopCurrentAudio()` تُستدعى في بداية
   الثلاث دوال.
2. **الصوت يقول جملة مختلفة عمّا هو مكتوب** (مثلاً يُكتب "يا لكِ من عبقرية
   خطيرة" ويُسمَع "أحسنتِ أيتها التلميذة"): `playPraiseAudio` كانت تكتشف
   مستوى التهنئة بمطابقة بادئات **مذكّرة فقط** (`يَالَكَ`، `أَحْسَنْتَ`...)
   حتى مع نص مؤنث — فتفشل المطابقة دائماً لأعلى مستويين مع البنات وتُشغِّل
   صوت المستوى الأول خطأً. أضفتُ `tiers_f` بصيغها الصحيحة.
3. **لا صوت إطلاقاً عند ظهور شارتَي "خطّاط الشكل"/"كاتب الكلمات"**:
   `checkWritingBadges` يضع `audioPath` على كائن الشارة نفسه، لكن
   `showBadgePopup` كانت تقرأه من `opts.audioPath` (خطأ تسمية) فتسقط دائماً
   للمسار الافتراضي العام `audio/badges/glyph_master.mp3` غير الموجود أصلاً
   (الملفات الفعلية لكل حرف/جنس مثل `glyph_master_ط_f.mp3`). صحّحتها لتقرأ
   `badge.audioPath` أولاً.

**منهجية التحقّق**: بدل الاكتفاء بقراءة الكود، استخدمت CDP لاستدعاء الدوال
مباشرة على التطبيق الحي وفحص `window._currentAudio.src` قبل/بعد كل إصلاح —
هذا ما كشف مشكلة `opts.audioPath` (لم تكن ستُكتشف من قراءة الكود وحدها بهذه
الثقة). راجع قسم "أدوات التصحيح" أدناه لتكرار المنهجية.

## إصلاح لاحق (نفس الجلسة): قبول أشكال حروف خاطئة تماماً في اختبار الكتابة

المستخدم أظهر لقطات من **التطبيق الحقيقي على أندرويد** (لا المتصفح) لأسئلة
"اكتب شكل حرف السين" في مواضعها الخمسة — كل الأشكال المرسومة كانت خربشات
ناعمة بلا أسنان إطلاقاً (لا تشبه السين البتة) وكلها قُبلت كإجابة صحيحة.

**السبب الجذري** (أثبتّه تجريبياً عبر CDP قبل الإصلاح، انظر ملفات الاختبار في
scratchpad إن احتجت تكرارها): `checkWriting()` — الفحص الهندسي المستخدم **لكل
أسئلة شكل الحرف المفرد على كل المنصّات** (حتى أندرويد؛ ML Kit مرتبط فقط بأسئلة
الكلمات الكاملة في `checkWordNative`، لا بالحروف المفردة إطلاقاً) — يمدّد خط
الطفل ×9 قبل حساب التغطية `cov`/الدقة `prec`. هذا التمديد الهائل يجعل أي
خربشة تملأ نفس صندوق الحرف تقريباً تسجّل `cov`/`prec` قريبة من 1.0 **بصرف
النظر عن وجود الأسنان فعلاً** — أثبتّ رقمياً أن خربشة بلا أي تعرّج تسجّل نفس
درجة حرف حقيقي تقريباً.

**الإصلاح**: أضفتُ فحصاً طوبولوجياً جديداً (`_pathReversalCount` +
`_glyphMainSkeleton`) يحسب عدد مرات تغيّر اتجاه الخط رأسياً (لأعلى/لأسفل)
بسعة معتبرة (يتجاهل ارتعاش اليد). إن كان الشكل المرجعي يحتوي تعرّجاً حقيقياً
(≥3 انعكاسات — يشمل معظم الحروف: س ش ص ث ن ق ي ت ع...، ويستثني الحروف
البسيطة كالألف والراء والواو) يُشترط أن يُظهر خط الطفل تعرّجاً حقيقياً أيضاً
(≥2 انعكاسات) وإلا تُرفض الإجابة بصرف النظر عن نتائج cov/prec. **لا يمسّ هذا
أسئلة الكلمات إطلاقاً** (تلك تمر بـML Kit الحقيقي على أندرويد أصلاً).

تحقّقتُ حيّاً على الـAPK المُعاد بناؤه: 5 مواضع للسين — كل الخربشات المزيّفة
(بلا أسنان) صارت تُرفض في الخمسة، ومحاولة تحتوي أسناناً حقيقية (مُصطنعة
يدوياً لأغراض الاختبار، مستقلة عن آلية توليد المرجع) قُبلت بنجاح في 4 من 5
(الفشل الوحيد كان بسبب ضعف تغطية الشكل المصطنع نفسه — قيد موجود مسبقاً في
cov وليس ناتجاً عن هذا الإصلاح).

⚠️ **هذا لا يزال فحصاً هندسياً تقريبياً، وليس تعرّفاً حقيقياً كـML Kit**. لم
أختبره ضد خط أطفال حقيقي متنوّع — يُحتمل أن يحتاج ضبطاً إضافياً لعتبة
الانعكاسات (حالياً: `tplRev>=3` يُفعّل الفحص، `childRev>=2` مطلوب) إن ظهرت
حالات رفض خاطئ لكتابة صحيحة فعلاً. الحل الأشمل (تمديد ML Kit ليغطي أشكال
الحروف المفردة أيضاً لا الكلمات فقط) يبقى خياراً مؤجَّلاً — المستخدم لم يقرّر
بعد.

**تحديث (نفس اليوم)**: المستخدم أثبت عملياً — بضربة حقيقية على حرف السين
(حلقة + ذيل متعرّج، بصرياً خاطئة تماماً) — أن الفحص أعلاه لا يزال يُخدَع. السبب
الجذري: كنت أرتّب نقاط رسم الطفل حسب الإحداثي الأفقي (x) قبل حساب التعرّجات،
وهذا يُفسد أي شكل يحتوي **حلقة** (كالصاد ونفسه) لأن الخط يتقاطع مع نفسه، فينتج
مئات التعرّجات الوهمية من مجرد تشابك ترتيب الفرز (أثبتّ رقمياً: نفس الضربة
سجّلت 31 "تعرّجاً" بالفرز x مقابل 3 فقط بالترتيب الزمني الحقيقي) — فيتجاوز أي
عتبة صغيرة بسهولة. **الإصلاح**: استخدام ترتيب الرسم الزمني الفعلي بدل الفرز
حسب x، ورفع الحدّ المطلوب ليكون نسبياً (`childRev >= max(2, tplRev-1)`) بدل
رقم ثابت صغير (2) يسهل تجاوزه. تحقّقتُ حيّاً: الضربة الحقيقية التي مرّت خطأً
تُرفض الآن، وكل اختبارات السين والصاد (بأشكالها الخمسة) سليمة.

**تنبيه صريح للمستخدم وللمحادثة القادمة**: هذا النمط (فحص هندسي يُخدَع، أُصلحه
جزئياً، ثم يظهر خدع جديد) قد يتكرر مع حروف أخرى (الحروف ذات الحلقات مثل ف ق و،
أو ذات التعرّجات المعقّدة). كل إصلاح حتى الآن رد فعل على دليل حقيقي وليس
تخميناً استباقياً — إن ظهرت حالة قبول خاطئ لحرف آخر، كرّر نفس المنهجية: التقط
`_wStrokes` الحقيقية عبر CDP فور حدوث القبول الخاطئ (قبل أن يُمسحها السؤال
التالي)، لا تُخمّن. الخيار الأشمل (ML Kit لأشكال الحروف المفردة، لا الكلمات
فقط) يبقى مطروحاً إن تكرّرت هذه الأنماط كثيراً — المستخدم لم يقرّر بعد.

**كذلك لوحظ (غير مُصلَح، ينتظر قرار المستخدم)**: نفس checkWriting الهندسي هو
ما يُستخدم لأسئلة الكلمات أيضاً عند الاختبار عبر **متصفح عادي** (فتح الملف
مباشرة) لأن `window.Capacitor` غير موجود هناك فيسقط `checkWordNative` تلقائياً
لنفس الفحص الضعيف — هذا سلوك متوقّع وموثّق مسبقاً، وليس خللاً جديداً. للاختبار
الحقيقي لدقة تعرّف الكلمات يجب استخدام الـAPK على أندرويد لا المتصفح.

## إعادة تصميم شاملة لفحص أشكال الحروف المفردة (نفس اليوم، تطوّر لاحق)

بعد أن استمر المستخدم بإيجاد ثغرات جديدة (خربشة حقيقية بحلقة+ذيل قُبلت خطأً
لحرف السين)، طلب مني اتخاذ القرار النهائي الأفضل بدل الترقيع التفاعلي. الخطوات:

1. **جرّبت تمديد ML Kit للحروف المفردة** — فشلت الفكرة: أثبتُّ عبر استدعاء
   `HW.recognize` مباشرة أن ML Kit يُطبّع كل أشكال الحرف الموضعية لنفس الرمز
   النصّي (لا يميّز "س" منفصلة عن "سـ" بداية) — لا يصلح وحيداً للتحقق من صحة
   *شكل الموضع* تحديداً.
2. **جرّبت خوارزمية DTW/مطابقة مسار** (تنحيف Zhang-Suen + استخراج مسار مرتّب +
   مقارنة شكلية) — لم تتفوّق على الفحص الحالي في الاختبار (نتائج غير متسقة
   عبر حروف بسيطة، وتعطّل NaN) — **لم تُنشَر**.
3. **فحص شامل تلقائي**: بدل انتظار أخطاء المستخدم، فحصت الحروف الـ33 كافة
   وكل مواضعها (792 اختباراً) ضد 6 أنماط خربشة (بينها حلقة+ذيل، تعرّج فوضوي،
   خربشة صغيرة) — النتيجة: **~80 حالة قبول خاطئ (10%)**، أبرزها حرف الحاء
   (بلا "أسنان" فتخطّى فحص التعرّج بالكامل) وأنماط "الخربشة الصغيرة"
   (تطبيع `_maskFromCanvas` يُكبّر أي رسمة تملأ الإطار بصرف النظر عن حجمها
   الحقيقي على اللوح).
4. **الحل النهائي المنشور** — طبقتان إضافيتان فوق الفحص الهندسي القائم:
   - **حدّ أدنى لحجم الرسم الحقيقي على اللوح** (`_inkBBoxFraction >= 0.22`
     من أي بُعد) يمنع "الخربشة الصغيرة".
   - **طبقة تحقّق عبر ML Kit** (`checkGlyphNative` جديدة): بعد نجاح الفحص
     الهندسي، تحقّق إضافي أن أعلى احتمال من `HW.recognize` هو الحرف المستهدف
     فعلاً (وليس أي حرف عشوائي شكله قريب) — يتراجع تلقائياً للفحص الهندسي
     وحده إن تعذّر الوصول لـML Kit.
   - أُعيد الفحص الشامل (792 اختباراً): **حالة واحدة فقط متبقية** (الراء —
     حرف بسيط جداً هندسياً، وML Kit نفسه أخطأ في تصنيف الخربشة كراء بالمصادفة).
5. **اكتُشف رفض خاطئ حقيقي بعد النشر**: المستخدم رسم كافاً حقيقية صحيحة
   (بضربتين منفصلتين: الجسم + الخط القطري الزخرفي) في وضعَي "وسط/نهاية متصل"،
   ورُفضت. التقطتُ بيانات الضربة الحقيقية عبر CDP وشخّصتُ: كل الفحوص نجحت
   (تغطية 0.53، دقة 0.95، تناسب 1.4، حجم 0.59) **إلا** فحص "التعرّج" (تطلّب
   ≥2 انعكاسات رأسية، والضربة الحقيقية سجّلت 1 فقط) — لأن عدّاد التعرّج يقيس
   التذبذب الرأسي *ضمن نفس الضربة*، وهو مقياس صحيح لحروف الأسنان (سين/شين)
   لكنه خاطئ للكاف: تعقيدها الحقيقي **ضربة قلم منفصلة تماماً** (الخط القطري)
   لا تذبذباً رأسياً. **الإصلاح**: إن استخدم الطفل ضربتين منفصلتين فأكثر (رفع
   القلم عمداً)، يُعفى من شرط التعرّج تماماً (دليل كافٍ على بناء متعمَّد متعدد
   الأجزاء). تحقّقت: الضربة الحقيقية تُقبل الآن، والفحص الشامل (792 اختباراً)
   ما زال سليماً، واختبرت أيضاً تقسيم خربشات عشوائية إلى ضربتين متعمَّداً
   للتحايل — لم ينجح (طبقتا cov/prec/ML-Kit تبقيان تحميان بصرف النظر عن عدد
   الضربات).

**الوضع الحالي (مُحدَّث بعد تحقّق شامل ببيانات حقيقية — انظر القسم التالي)**:
مُختبَر بعمق ضد الخربشات وأيضاً ضد 471 عيّنة خط حقيقي جمعها المستخدم بنفسه.

## تحقّق نهائي ببيانات خط أطفال حقيقية (471 عيّنة) — إصلاحات جوهرية إضافية

المستخدم بنى أداة جمع بيانات مستقلة (`scratch_letter_collector.html` في جذر
المشروع) وجمع بنفسه **471 عيّنة رسم حقيقية** عبر 33 حرفاً و131 من أصل 132
موضعاً ممكناً (ملف JSON محلي، ليس في المستودع). شغّلتها كلها عبر الفحص الفعلي
الحقيقي (`checkGlyphNative`) على الجهاز — كشف هذا عن **3 أخطاء جوهرية جديدة**
لم تكن لتظهر بأي اختبار اصطناعي:

1. **فحص "الأسنان/التعرّج" حُذف بالكامل نهائياً** — أثبتت 96 من 471 عيّنة
   حقيقية (رفض 20.4%!) أن هذا الفحص يرفض خطأً حروفاً صحيحة تماماً (خصوصاً
   الصاد والعين والهاء والثاء — حروف حلقة/خطاف لا "أسنان" حقيقية، رغم اجتيازها
   كل الفحوص الأخرى بتغطية 0.85-0.99). تحقّقت أن طبقة ML Kit وحدها (مع
   cov/prec/aspect/dens) تكفي لرفض نفس الخربشات الأصلية التي دفعت لبناء هذا
   الفحص أول مرة (بما فيها ضربة "السين" الخربشة+الحلقة نفسها) — فحُذف الفحص
   ودالتاه المساعدتان (`_pathReversalCount`, `_glyphMainSkeleton`) نهائياً
   بدل الاستمرار بترقيعه حرفاً بحرف.
2. **حدّ الحجم الأدنى خُفِّض من 0.22 إلى 0.15** — نفس البيانات أظهرت حروفاً
   مدمجة كالهاء والميم مرسومة بعناية (تغطية 0.95-0.99!) تُرفض فقط لأن نسبة
   إشغالها للوح 0.18-0.21، وهذا كان تحت الحدّ 0.22 الذي رفعته سابقاً استجابة
   لحالة تحايل ضيقة (خربشة صغيرة صدف أن ML Kit صنّفها "هاء"). الحدّ الجديد
   0.15 يفصل بأمان بين خربشة اصطناعية فعلية (0.11) وحروف حقيقية مدمجة (0.18+).
   ⚠️ **مقايضة واعية**: هذا يُعيد فتح ثغرة ضيقة (خربشة دائرية صغيرة لحرف الهاء
   تحديداً تمرّ في كل مواضعه الخمسة + حالة واحدة للميم — 6 من 342 في إعادة
   فحص التحايل) — قرَّرتُ قبولها لأن كلفة رفض إجابات صحيحة حقيقية لأطفال
   (20.4%) أفدح بما لا يُقارن من كلفة قبول خربشة نادرة الحدوث فعلياً.
3. **فحص النقاط أصبح يتسامح مع "النقطة المدموجة" حتى للحروف التي تتطلّب
   نقاطاً** (كان يتسامح فقط عندما لا تحتاج الحرف نقاطاً أصلاً، عكس المنطق
   المطلوب) — إن اندمجت نقطة الطفل بجسم الحرف (شائع جداً) ولم تُكتشَف كمكوّن
   منفصل، يُقبل ذلك بشرط أن التغطية العامة ≥0.75 (دليل أن الحبر موجود فعلاً).

**النتيجة النهائية**: نسبة قبول العيّنات الحقيقية الـ471 قفزت من **79.6% إلى
99.2%** (4 عيّنات متبقية فقط: 2× شين البداية، 1× جيم النهاية بعد متصل، 1×
جيم النهاية بعد غير متصل/نقاط — لم أُشخّصها بعمق، قد تكون عيّنات فعلاً غير
دقيقة وليست خللاً في الفحص، لم أُحقّق من هذا بعد).

**لتكرار هذا النوع من التحقّق مستقبلاً**: ملفات التوليد/التشغيل بالدفعات
(`gen_sample_batches.js`, `sbatch_*.js`) في مجلد scratchpad الجلسة — تحتاج
ملف `horofi_samples.json` (تصدير من أداة الجمع) وإعادة توجيه منفذ CDP كالمعتاد.
⚠️ درس تقني مهم: عند بناء لوح رسم وهمي لإعادة تشغيل ضربات محفوظة، يجب تحديد
حجم عنصر `<canvas>` نفسه صراحة (`style.width/height`) — `_drawStrokesCanvas`
يقرأ `_wCanvas.getBoundingClientRect()` **مباشرة** لا حجم الحاوية الأب، وأي
canvas بلا تنسيق CSS صريح يرجع للحجم الافتراضي 300×150 بصرف النظر عن حجم أبيه،
ما يُفسد أي إحداثيات تتجاوز ذلك (اكتشفنا هذا بعد أن أعطى كل شيء "empty" خطأً).

إن ظهر رفض خاطئ جديد لحرف آخر مستقبلاً: أعد تشغيل الفحص الشامل بالعيّنات
الحقيقية إن كانت لا تزال متوفرة (أدقّ بكثير من أي اختبار اصطناعي)، أو التقط
`_wStrokes` عبر CDP فور الرفض (قبل الانتقال للسؤال التالي)، شخّص أي شرط فشل
تحديداً (استخدم نمط `gen_diag.js` في scratchpad كقالب)، ثم أصلح الشرط تحديداً
بدل تخمين حلّ عام جديد.

## ⚠️ تصحيح جوهري: طبقة ML Kit أُزيلت من فحص أشكال الحروف المفردة

**خطأ قياس اكتُشف لاحقاً**: رقم "99.2% قبول" المذكور أعلاه كان قد قِيس بأداة
اختبار معطوبة — سكربت الدفعات كان يُعيد `CL` لقيمته السابقة *قبل* استدعاء
`checkGlyphNative`، وهذه الدالة تقرأ `CL.char` لمقارنة ناتج ML Kit بالحرف
المستهدف. فكانت المقارنة إمّا ضد حرف خاطئ أو ترمي استثناءً يُلتقط بـcatch
فيُرجِع الفحص الهندسي وحده — أي أن القياس كان **يتخطّى طبقة ML Kit كلياً دون
أن يظهر ذلك**. بعد إصلاح الأداة وإعادة القياس على نفس الـ471 عيّنة:

| النهج | قبول كتابة أطفال حقيقية صحيحة | رفض الخربشات المزيّفة (792 اختبار تحايل) |
|---|---|---|
| هندسة فقط | **99.2%** | 84.6% (122 قبول خاطئ) |
| هندسة + ML Kit (أول احتمال) | **41.4%** ✗ | 98.25% |
| هندسة + ML Kit (ضمن الاحتمالات) | 67.9% ✗ | — |

**السبب جوهري لا معايرة**: ML Kit مُدرَّب على كتابة طبيعية *داخل سياق كلمة*؛
أما شكل موضعي معزول (مثل "بـ" وحده) فيقرؤه ل/ر/د. في 32% من الحالات الصحيحة
لم يذكر الحرف الصحيح ضمن كل احتمالاته العشرة إطلاقاً — فلا يُصلحه قبول ترتيب
أدنى ولا تعديل عتبة.

**القرار**: أُزيلت الطبقة من `checkGlyphNative` (صارت تُرجع `checkWriting`
مباشرة) وبقيت عاملةً في `checkWordNative` (أسئلة الكلمات — مُدخَل طبيعي للنموذج
وقد أثبتت نفعها هناك بمثال "قلم/قمم"). المقايضة المقبولة صراحةً: ~15% من
الخربشات المتعمّدة تمرّ، مقابل عدم إحباط الطفل برفض إجابته الصحيحة — وهو
الأخطر بكثير في تطبيق أطفال.

**درس منهجي**: أي أداة اختبار تحتوي `try/catch` يرجع لمسار احتياطي قد تُخفي
أن الطبقة المُختبَرة لم تعمل أصلاً. تحقّق دائماً أن ما تقيسه هو فعلاً المسار
المقصود (اطبع ناتج الطبقة نفسها لا النتيجة النهائية فقط).

## مرحلة جديدة: تجميع الكلمة + مسار كتابة مباشر (28.07.2026)

**1) تمرين تجميع الكلمة (`kind:'assemble'`)** — يسبق الكتابة اليدوية في
**كلا المسارين**. نفس سؤال "اكتب الكلمة موصولة"، لكن بدل الرسم تظهر لوحة بطاقات
تضم كل أشكال حروف الكلمة (مصفوفة صفاً لكل حرف، الأشكال مخلوطة داخل الصف كي لا
يُخمَّن الترتيب)، فيبني الطفل الكلمة بالنقر وكل حرف يُطبع بلون مختلف. أزرار
تراجع/مسح/اسمع الكلمة، محاولتان ثم كشف الكلمة الصحيحة وخصم قلب.

**ميزته الجوهرية**: التحقق مقارنة رموز يونيكود مباشرة (`_assembleExpected`
تستخدم `getNaturalForm` نفسها المستخدمة في جولات التمييز) — **صواب/خطأ قاطع
100% بلا عتبات ولا تعرّف ضبابي**. وهو المعالجة الحقيقية لضعف الفحص الهندسي:
يفصل مهارة "أي شكل يناسب هذا الموضع؟" (معرفة) عن "كيف أرسمه بيدي؟" (حركية).

توزيع الجولات صار: 5 أشكال حرف ← 3 تجميع ← 2 كتابة يدوية (كان 5 يدوية)،
فالمجموع 10 كما كان ولم تطُل الجلسة. الكلمات الأقصر تُسنَد للتجميع لأن عدد
البطاقات = مجموع أشكال حروف الكلمة (10-12 بطاقة لكلمة ثلاثية).

**2) مسار مباشر لتمارين الكتابة** (`startWritingOnly`) — قسم جديد أسفل صفحة
الحروف (`lgridWrite`) يدخل مباشرة لمراحل الكتابة الثلاث بلا جولات التمييز الـ15.
الشبكات صارت `compact` (5 أعمدة، بطاقات أصغر) لإفساح المساحة.

⚠️ **عائق تقني كان سيُفرِغ المرحلة**: `buildWritingRounds` يشتق كلماته الخمس من
`ROUNDS` (جولات التمييز)، فالدخول المباشر كان سيُنتج أسئلة أشكال فقط بلا أي
كلمة. الحل: تُولَّد `ROUNDS` في الخلفية كمصدر كلمات دون عرضها.

**فروق المسار المباشر** (عبر راية `WRITING_ONLY`): المجموع يستثني نقاط التمييز
(وإلا لظهرت نتيجة كاملة كـ"150 من 300")، والتفصيل يعرض الكتابة فقط، و**لا
يُمنح إتقان الحرف ولا شارة ✅** لأن الإتقان في هذا التطبيق يعني المرحلتين معاً
(نفس القاعدة الموثّقة أعلاه) — والرسالة تقول "تدرّبتَ على كتابة حرف X" بلا
ادّعاء. وتُتخطّى أصوات mastery/close لأنها كلها تنطق "إتقان الحرف".

## بنية المشروع

- **الملف الرئيسي**: `horofi-v11-9-29.html` — ملف واحد ضخم (HTML+CSS+JS)، هذا هو
  مصدر الحقيقة الوحيد. أي تعديل يُنسخ بعده إلى `android-app/www/index.html` ثم
  `npx cap sync android` ثم بناء Gradle.
- **مشروع أندرويد**: `android-app/android/` (Capacitor). إضافة أصلية مخصّصة:
  `android-app/android/app/src/main/java/com/horofi/app/HandwritingPlugin.java`
  (تعرّف كتابة يدوية حقيقي عبر Google ML Kit — راجع قسم "الميزات الكبرى" أدناه).
- **الصوت**: مجلد `audio/` بالجذر (يُنسخ لـ`android-app/www/audio/`). كل سكربتات
  توليد الصوت بصيغة `horofi_tts_*.py` بالجذر، تستخدم ElevenLabs API (المفتاح
  عند المستخدم فقط، لا يُشارك في المحادثة أبداً).
- **دورة العمل المعتادة لأي تعديل**:
  ```
  cp horofi-v11-9-29.html android-app/www/index.html
  cd android-app && npx cap sync android
  cd android && JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew assembleDebug
  ```
  ثم تثبيت الـAPK على المحاكي عبر adb.

## أدوات التصحيح المهمة (اختصرت وقتاً كبيراً هذه الجلسة)

بدل النقر اليدوي عبر adb (بطيء وعرضة للخطأ في الإحداثيات)، استخدمت **Chrome
DevTools Protocol** مباشرة على WebView التطبيق:
```bash
ADB="/c/Users/m-hm1/AppData/Local/Android/Sdk/platform-tools/adb.exe"
"$ADB" shell cat /proc/net/unix | grep -o "webview_devtools_remote[^ ]*"  # اسم socket
"$ADB" forward tcp:9333 localabstract:webview_devtools_remote_XXXXX
curl -s http://localhost:9333/json  # يعطي webSocketDebuggerUrl
```
ثم استخدام `cdp_eval.js` (موجود في مجلد scratchpad الجلسة، أعد كتابته إن لزم —
سكربت Node صغير يستخدم `WebSocket` الأصلي المدمج في Node 22+ بدون أي حزمة
خارجية، يرسل `Runtime.evaluate` عبر WebSocket:
`node cdp_eval.js "<wsUrl>" "<jsExpression>"`) لتنفيذ أي JS
مباشرة داخل التطبيق الحي: تخطي جولات، محاكاة رسم بـ`PointerEvent`، فحص متغيرات
عامة (`WROUNDS`, `WTRIES`, `answered`, إلخ) دون أي نقر يدوي.

هذا أسرع وأدق بكثير من `adb shell input tap` (الذي سبّب أخطاء إحداثيات متكررة
بسبب معامل تحجيم الشاشة 1.2× بين لقطات الشاشة والإحداثيات الحقيقية).

## الميزات الكبرى المُنجَزة هذه الجلسة (بالترتيب الزمني التقريبي)

1. إصلاحات واجهة عامة: شريط الحالة، زر الرجوع الفعلي، رسائل الإتقان، تحسينات تصميم
2. الشخصية المرافقة (mascot_happy.png / mascot_sad.png) في عدة شاشات
3. صفحة هبوط كاملة على GitHub Pages (`index.html` بالجذر) بحالة "قبل/بعد الإطلاق"
4. **مرحلة اختبار الكتابة الكاملة**: بعد 15 جولة اختيار من متعدد، يكتب الطفل
   أشكال الحرف ثم كلمات كاملة بإصبعه على لوح Canvas
5. **تعرّف كتابة يدوية حقيقي (ML Kit)**: المقارنة الهندسية البسيطة فشلت (قبلت
   "قمم" خطأً بدل "قلم")، فبنينا إضافة Capacitor أصلية تستخدم
   `com.google.mlkit:digital-ink-recognition:18.1.0` (⚠️ لا ترقّها لـ19.0.0 —
   غيّرت واجهة البرمجة بالكامل وتحتاج minSdk 23). تعمل على أندرويد فقط حالياً.
   نموذج اللغة العربية يُنزَّل تلقائياً عند فتح التطبيق (يحتاج إنترنت أول مرة).
   ⚠️ **حُذفت نهائياً (28.07.2026)** بعد أن صارت كوداً ميتاً غير مستخدَم — راجع
   قسم "4 تحسينات على تدريب التتبّع" أعلاه. لا وجود لها في الكود بعد الآن.
6. إصلاح خطأ جنس حقيقي: التسجيل كان يسمح بالمتابعة دون اختيار صريح لـ"ولد/بنت"
   (افتراض صامت لـ"ولد"). أصبح الاختيار إلزامياً الآن (`_obGenderChosen`).
   ⚠️ أي طفل مسجَّل قبل هذا الإصلاح قد يحمل جنساً خاطئاً محفوظاً فعلياً —
   يحتاج الوالد تصحيحه يدوياً عبر "تغيير الاسم".
7. **نظام مكافآت مُوسَّع**: شارتان جديدتان لكل حرف (glyph_master, word_master)
   عبر آلية منفصلة (`ch.writingBadges`) لا تمسّ نظام `_lvlBadges` القديم. نقاط
   الكتابة +15 بدل +10. ترقية إصدار تلقائية (`REWARDS_VERSION=2`) تصفّر
   النقاط/الشارات مرة واحدة فقط لكل طفل محفوظ (لا تمسّ تقدّم الحروف المُتقنة).
8. **فصل شاشتَي النتيجة**: بعد الجولات الـ15 → شاشة نتيجة وسيطة منفصلة (بلا أي
   ادّعاء إتقان) بزر "ابدأ اختبار الكتابة"، ثم شاشة نتيجة نهائية بعد الكتابة.
   رسالة "أتقنتَ الحرف" ممنوعة إلا عند 100% عبر المرحلتين معاً.
9. تدقيق شامل لكل نصوص الجنس (`g()` helper) — سليمة كلها ما عدا نقطة التسجيل.
10. توليد صوت شامل: 123 ملفاً لأسئلة الكتابة، 112 لشارتَي الكتابة الجديدتين،
    10 لشاشات النتائج (بطل حروفي، نتيجة جزئية، إلخ) — كلها بصيغتَي مذكر/مؤنث.

## معروف/مؤجَّل

- تحذير توافق 16KB لمكتبة `libdigitalink.so` (من Google، ليست مكتبتنا) — غير
  حاسم حالياً (تحذير فقط)، لا حل متاح دون كسر الميزة، مؤجَّل حتى تُصدر Google
  نسخة متوافقة موثّقة.
- قائمة كاملة لما قبل الإطلاق موجودة في ملف الذاكرة الدائم (راجع
  `~/.claude/projects/.../memory/horofi_prelaunch_checklist.md`).

## نصيحة للمحادثة الجديدة

اطلب من كلود قراءة هذا الملف أولاً (`Read HANDOFF.md`) بدل إعادة شرح كل شيء.
احذف الأقسام المُنجَزة/القديمة من هذا الملف تدريجياً كي يبقى مختصراً ومفيداً.
